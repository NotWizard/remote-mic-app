# TODO

## 代码审计整改台账（A1–A10）

来源：2026-08-21 五领域并行代码审计。每项修复后都经过一个独立复核轮次实跑验证命令并有否决权；台账与仓库任何位置都未记录 Agent 身份，因此只能证明复核是独立轮次，不能据此断言实施与复核由不同 Agent 执行。合同要求「每项独立 commit」，实际 A7–A10 各自需要多个后续 commit 才收敛（见下表 Commit 列）。会话起始基线：`swift test` 247 项、`scripts/test.sh` 42 项；当前实测 `swift test` **392 项 / 34 套**（命令：`swift test`，末行 `Test run with 392 tests in 34 suites passed`）。26 提交整改收尾时为 391 项；整改复核轮把 1 项源码文本断言转为行为断言（拆成 1 个行为测试 + 1 个瘦身的源码文本测试，净 +1 个测试函数），故由 391 增至 392。

| 编号 | 状态 | 问题 | 主要位置 |
| --- | --- | --- | --- |
| A1 | **已修复** | 10 处 `try?` 解码失败静默重置用户配置，无日志无 UI，把"首次无数据"与"解码失败"混为一谈。已闭合三部分：日志、`.corrupt` 原始字节备份、按键映射页内联提示。**定位为防御性加固**——无证据表明已有用户命中解码失败，此前那次配置丢失的根因是 HID 就绪（见 `Bugs/2026-08-21-remote-reconnect-loses-custom-button-mapping.md`） | `Sources/RemoteMic/AppSettings.swift` 449–565 与约 690（`firstUseEvents`） |
| A2 | **已提交** `858bb9c` | `killall coreaudiod` 在 `set -euo pipefail` 下无匹配进程即中止脚本，安装/卸载已完成却报失败 | 4 个安装脚本 |
| A3 | **已修复** | 日志无轮转、每行开关文件、权限 644；`write` 非 `@autoclosure` 且释放路径缺幂等门，单条消息占日志 47%。折叠经复核否决后改为显式白名单（原启发式会把 1586 行内容不同的消息错误合并，等于把 A6 缺陷推广到 220 类消息）。投影：113242 → 53823 行（−52.5%）、21.49 → 10.59 MB（−50.7%） | `AppLogger.swift:18`、`BridgeAppModel.swift:2074` |
| A4 | **已修复** | 每轮重建 central + 自建 8s 超时 + 3s 重试，遥控器缺席时 315 次/小时空转；`connect()` 本就永久挂起。改为单 central 常驻、待连请求不再被自我取消、8s 截止时间只改显示状态。缺席 1 小时的连接尝试 **328 → 1**，24 小时 **7855 → 1**（由策略延迟推导，非断言）。复核否决后补两处：唤醒时重新发起（被删掉的 8s 循环原本是睡眠后唯一自愈路径）、`.unauthorized` 分支复位 lifecycle | `XiaomiBluetoothBridge.swift`、`BluetoothLifecycle.swift`、`RemoteMicApp.swift:244` |
| A5 | **已修复** | `drainCompletion` 单槽被置 nil 却从不调用且作废兜底定时器，`.draining` 无超时出口，语音会话可永久卡死。改为一次性终结语义：`flushPlayer`/`stop`/重复请求都必须先取出再调用；`.draining` 另加 2 秒代次守卫兜底（`AudioOutput` 的兜底闭包持弱引用，实例释放后不会送达任何回调）。附带修好手机路径 `VoiceFunctionKeyLatch` 因回调丢失而永久按下的问题 | `AudioOutput.swift`、`VoiceFnTapSessionController.swift` |
| A6 | **已修复** | 关键 `return` 无日志或多种原因压成同一条，线上无法定位。新增 21 个 case 的 `HIDSuppressionReason`，覆盖审计点名的全部 4 处；逐报告热路径经 `logInputIgnored` 以整条消息为折叠键（不同按键/usage 各自成键，不会互相合并），实测 40 次按键由 40 行降为 1 行。既有 `mapping_enabled=` / `power_suppressed=` 字段保留 | `HIDRemoteMonitor.swift`、`RemoteButtons.swift`、`AppLogger.swift` |
| A7 | **已修复（行为覆盖完成，文本断言部分转换）** | 蓝牙桥零行为测试已闭合：抽出 `ATVVVoiceSessionCore`（连接周期留在桥内，语音会话移入核心并返回有序 effect），新增 11 项事件回放测试，覆盖 RC003 无前置 `MIC_OPEN` 基线、流中断连、迟到代次回调（含正向对照）。**文本断言已开始转换**：先证明了它们确实什么都不管——`b308287` 改了 366 行 `BridgeAppModel`（含删掉一处 main hop），而断言 `guard self.isPhoneRemoteConnectionEnabled else` 只要那句话的字面还在就继续全绿。已把 33 项文本断言换成 65 项行为断言（连接开关与迟到授权、语音来源互斥、Watch 侧镜像），7 次负向对照实测「旧绿新红」；为此把 `BridgeAppModel` 7 个声明由 `private` 放宽到 internal（`@testable` 不穿透 `private`），零逻辑改动。**余下未转的源码文本（A 类）断言此前用两个不同口径都叫「余下」才自相矛盾，现分清**：全部 A 类（断言 `Sources/*.swift` 文本）约 223 项，其中 `SettingsPageRegressionTests.swift` 一个文件约 145 项——后者是前者的**子集**，不是两个互斥的「余下」。该普查高度依赖口径（是否计 `#require`、是否按循环展开、是否计间接派生的切片），`Bugs/2026-08-21-bluetooth-bridge-had-no-behaviour-coverage.md` 已把该文件定为 **173–182 的区间**而非精确值，不同 `grep` 口径实测得 79/89/137 等不同数，因此这里的 223/145 是量级而非精确计数。本轮又转 2 项（电池图标与配色的判定原先只断言源码字面 `if level <= 10 { return .red }`，现抽出 `RemoteBatteryPresentation` 并在 nil/0/10/11/25/26/50/51/75/76/100 每个边界实测，负向对照 3 次「旧绿新红」）；整改复核轮再转 1 项（`watchBluetoothServer.updateButtonTitles(titles)` 由源码文本改为行为断言，见 `theButtonTitlesTheMacComputesReachTheWatchTransport`，负向对照删该行即红）。原先「需要仓库尚不具备的快照能力」的说法**已作废**——A9 证明仓库自带的离屏渲染器可用。按证据分类：**可转换**的是纯判定逻辑，办法就是抽出＋边界测试，属于可持续清理；**确实转不动**的是断言 SwiftUI 修饰符存在（`ViewThatFits`、`fillsWidth ? nil : 232` 这类，需要 view 自省能力）与模态 `NSAlert` 第三按钮（`connection.phone.cancel_waiting` / `alertThirdButtonReturn`，无可注入呈现器）；窗口几何与日志行断言本来就是行为断言，不在待转之列；本地化键已由 A9 的全局门禁接管。317 项 B 类守护构建脚本，本就没有运行期对应物 | `XiaomiBluetoothBridge.swift`、`BluetoothLifecycle.swift`、`BridgeAppModel.swift`、`Tests/` |
| A8 | **已修复** | 未标 `@MainActor`，**22** 个 `@Published` 属性声明（第 103–124 行；`grep -c "@Published"` 得 24，多出的 2 处在注释里——第 88、267 行，非属性，故属性数是 22 不是 24）靠 31 处手写 main hop 维持，`webRemoteClient.onStateChange`（`:355-357`）与 `onApprovalCancelled`（`:358-360`）已漏。已给类、`XiaomiBluetoothBridgeDelegate` 与桥标 `@MainActor`。**关键发现：光标 `@MainActor` 抓不到这个缺陷**——普通闭包字面量赋给供应商非 `@Sendable` 回调时会静默继承主 actor 隔离，连 `-strict-concurrency=complete` 都不报；因此 28 个回调字面量标了 `@Sendable`，跳转从此由编译器强制（删一个即硬错误）。hop 31 → 34（删 4 加 7，供应商是 stub 不承诺线程，多数 hop 并非冗余）。**原先刻意留下的那处已收口**：两个 `isIdentityTrusted` 必须同步返回，此前在传输线程读主线程会写的 `@Published` 字典。我最初把它记成「需改私有仓库协议」——**这是记错了**：协议改动只是把回调改成异步所需，让读取跨线程安全在本仓即可完成。现改为一把叶子锁维护的快照，`didSet` 与 `init` 迁移两条写路径都覆盖，快照先于持久化更新所以吊销立即生效。**不是修掉了已证实的崩溃**：TSan 在修复前后都报 0 条竞态，原因是 Combine 似乎用自己的锁串行化了 `@Published` 存储——这是未文档化的实现细节，不是保证；把读锁去掉后 TSan 确实报竞态，说明测试有检出能力。真正的价值是不再依赖那个细节，且 `AppSettings` 一旦标 `@MainActor`（本轮之后的自然下一步）这是同步查询唯一合法的形态 | `BridgeAppModel.swift`、`XiaomiBluetoothBridge.swift` |
| A9 | **已修复** | 53 处 10–11pt 中文改由 `InterfaceFonts` 统一 token 保证（12/13pt，测试守住下限）；两处授权弹窗（手机/手表与网页，审计只发现一处）硬编码中文改走本地化，中英各补 7 个键；`RemoteMappingCanvas` 的 270pt 卡片下限盖过中央通道，窄窗时卡片压在遥控器图上，改为按剩余宽度收缩（生产 1020 宽下结果不变）。第 4 项按你的选择处理：`AGENTS.md` 的界面门禁改为**跟随当前生产 `minSize`** 而不写死尺寸（git 历史显示门禁写下 110 分钟后 `minSize` 就被抬高，门禁才是过期物），更窄布局改用离屏渲染检查；`Testing/CustomApplicationFocus.md` 两条不可能成立的 `[x]` 记录已取消。**已补自动渲染门禁**：`SettingsPageRenderingTests` 用仓库自带的离屏渲染器把 5 个页面在 `1020×772` 与 `800×650`、中英双语、浅深两种外观下真实渲染，断言不崩、页数为 5、不改变窗口几何、映射页确实画出了遥控器图；负向对照删掉一个页面即 9 项失败。**但仍不能替代目视复核**：约一半界面字符串走 SwiftUI `Text("key")`，在测试进程里主 bundle 是测试宿主、没有 `.lproj`，这些字符串渲染成原始键名，因此**测试进程的**渲染结果无法验证字号与文案。**字号与布局的目视复核已于 2026-08-24 完成**：早先「App 二进制被 SIGKILL、本会话出不了图」的记录是错的——改用 `dist/Remote Mic.app` 里的二进制跑截图模式退出码为 0，中英各出 5 张 `1020×772` 真实文案图（App bundle 自带资源表）。实测结论：中文全为真实文案、无一处目视低于 12pt，遥控器照片未被两侧卡片覆盖，三个触发按钮不重叠不溢出，关于页 `1.8.25-fork.4` 在 28pt 下完整显示（版本号 6→13 字符的溢出风险由此排除），统计页此前出现的 `statistics.voice_ranking.title` 等原始键名确认为测试宿主缺资源的假象而非产品缺陷。命令与逐项观察见 [`InterfaceFontAndLocaleCompliance.md`](Testing/InterfaceFontAndLocaleCompliance.md) 用例 3C。**边界**：离屏渲染只验证文案、字号、布局与截断，窗口几何、拖拽缩放与实时交互仍需真机 GUI 会话。另补一道全局门禁：源码请求的 315 个本地化键必须在两种语言里都能解析——原先的键集对等检查只比两份文件，键从**两边同时删掉**照样通过，只有 7 个授权弹窗键有单独保护；负向对照实测：两边同时删一个键，新门禁报出该键与两种语言，旧对等检查仍为绿 | `SettingsView.swift`、`InterfaceFonts.swift`、`RemoteMappingCanvas.swift`、`BridgeAppModel.swift`、`AGENTS.md` |
| A10 | **已修复（本仓部分）** | 配置导入仅校验两个字段即接收任意 app 路径与快捷键；信任存储无过期无吊销。导入现按字段域逐条校验并丢弃不可信条目，结果经既有 `CorruptedSettingsNotice` 内联上报；信任条目加 30 天过期。复核否决了第一版的纯 ASCII bundle id 限制——脚本编辑器导出的中文名 App 标识本就含中文，实测会让**已装在本机**的 App 配置归零；同时把 bundle id 不一致从丢弃改为降级提示（执行路径本来就会再判等，丢弃只销毁绑定不增加安全性）。Keychain 密钥对与远程指令逐帧认证需改私有仓库协议，**不在本仓实现**，残余风险已记录 | `AppSettings.swift`、`SettingsView.swift` |

跨私有仓库的协议变更（A10 的信任改密钥、远程指令逐帧认证）不在本仓实现，只做本仓可收敛部分并记录。

### 执行记录（实施 / 复核 / 验证）

每项修复都经过一个独立复核轮次实跑验证命令并有否决权，否决过的项目退回重做后才提交；台账及仓库任何位置都未记录 Agent 身份，因此只能证明复核是独立轮次，不能证明实施与复核由不同 Agent 执行。其中 6 项（A1、A2、A3、A4、A6、A10）至少被否决过一次，合计 8 次否决。下表「实施 commit 数」为该项的实施提交数量；合同要求每项单一 commit，A7/A8/A9 均需 2–3 个 commit，A10 另与 A8 共享一个跨项 commit。

| 编号 | Commit | 实施 commit 数 | 复核结论 | 测试手册 | 验证证据 |
| --- | --- | --- | --- | --- | --- |
| A1 | `27571c3` | 1 | **否决 1 次**：`corruptedSettingKeys` 无任何消费方，"无 UI"那一半没闭合；另纠正数量 8→10、并推翻"该项导致过用户配置丢失"的说法 | [`CorruptedSettingsNotice.md`](Testing/CorruptedSettingsNotice.md) | `grep -c 'try? JSONDecoder'` = 0；`SettingsCorruptionRecoveryTests.swift` 6 项 |
| A2 | `858bb9c` | 1 | **否决 2 次**：① `verify-doubao-driver-pkg.sh` 仍断言旧的裸 `killall`，该脚本自身 `set -euo pipefail`，等于把被修的缺陷类型搬进了构建链；② `2>/dev/null` 吞掉 stderr，权限失败也会打印"服务未运行" | [`SplitInstallerArtifacts.md`](Testing/SplitInstallerArtifacts.md) | `build-driver-dmg.sh` 退出 0；`lsbom` 实测安装包 35 条、`Applications` 条目 **0**；PATH 注入必失败 `killall`/`pgrep` 后卸载脚本仍退出 0 |
| A3 | `43467f1` | 1 | **否决 2 次**：① "零 `AUDIO DEFAULT_INPUT` 事件"为假，投影脚本把 5 个门禁条件中的 2 个写成乐观常量；② 按"首个 `=` 前缀"折叠会把 1586 行内容不同的消息错误合并——等于把 A6 缺陷推广到 220 类消息 | [`RuntimeLogGovernance.md`](Testing/RuntimeLogGovernance.md) | 真实 11 天日志投影：113242 → 53823 行（−52.5%）、21.49 → 10.59 MB（−50.7%）；改后内容改变型合并实测 0 |
| A4 | `251d3f3` | 1 | **否决 1 次**：被删掉的 8 秒循环原本是睡眠唤醒后唯一的自愈路径，改后桥会停在 `.connecting` 且零待处理项；另发现 `.unauthorized` 分支同样死锁 | [`BluetoothAbsentRemoteReconnect.md`](Testing/BluetoothAbsentRemoteReconnect.md) | 缺席 1 小时连接尝试 **328 → 1**、24 小时 **7855 → 1**（由代码延迟推导）；负向对照：策略改回即 3 项失败 |
| A5 | `044f69f` | 1 | 通过（复核另指出该修复顺带修好了手机路径 `VoiceFunctionKeyLatch` 永久按下） | [`VoiceDrainInterruptionRecovery.md`](Testing/VoiceDrainInterruptionRecovery.md) | 4 次负向对照均 `completionCount → 0`；`.draining` 兜底失效即红 |
| A6 | `b0270f4` | 1 | **否决 1 次**（文档同步）：`TODO.md` 该行仍写"待开始" | 并入 [`HIDMappingReadinessRetry.md`](Testing/HIDMappingReadinessRetry.md) | 21 个 case 各有唯一 token；40 次按键由 40 行折叠为 1 行；去掉白名单即 2 项失败 |
| A7 | `550e685`、`1f821f3`、`e5740be` | 3 | 通过（第二轮复核纠正了 3 个被高估的数字：负向对照 issue 数、以及"独立参考解码"实际用的是同一个生产解码器。第三轮复核实测切片区间未缩小——旧界定符第 1396 行 + 插入 33 行 = 新界定符第 1429 行，同为 92 行——并查出实施轮次报告的还原 sha256 与两个文件都不匹配，以及台账里"需要仓库尚不具备的快照能力"已被 A9 推翻） | 并入相关手册 | 11 项事件回放；33 项文本断言 → 65 项行为断言，另加电池图标/配色 2 项按 11 个边界实测；7 + 3 次负向对照实测"旧绿新红" |
| A8 | `b308287`、`2c0ee31` | 2 | 通过。复核独立复现了关键结论：**单标 `@MainActor` 抓不到该缺陷**——普通闭包字面量赋给非 `@Sendable` 回调会静默继承隔离，`-strict-concurrency=complete` 也不报。`2c0ee31` 为后续 commit：把传输线程读取的信任列表改为叶子锁快照（不再依赖 Combine 内部），并新增并发读写测试（落在 A10 的 `ConfigurationImportValidationTests.swift`，与 A10 信任存储重叠） | 无（无用户可见行为变化） | 负向对照：还原原缺陷即 `mainThreadFlags` 非空；`build-app.sh` 退出 0 |
| A9 | `8b30824`、`a908e11`、`5c7a5b4` | 3 | 通过。复核另查出**第二条**同样被误标通过的记录：`CustomApplicationFocus.md` 曾勾选"中文字号均不小于 12pt"，而该页当时正有 51 处 10–11pt。后续 commit：`a908e11` 加自动离屏渲染门禁（5 页×两尺寸×双语×浅深），`5c7a5b4` 加本地化键解析全局门禁 | [`InterfaceFontAndLocaleCompliance.md`](Testing/InterfaceFontAndLocaleCompliance.md) | 53 处改由 token 保证；字号低于 12 即红；旧 `cardWidth` 恢复后 4 项几何测试红 |
| A10 | `3a0bd82`（+ 与 A8 共享 `2c0ee31`） | 1（+1 共享） | **否决 1 次**：纯 ASCII bundle id 限制会让**已装在本机**的中文名 App（脚本编辑器导出的 `com.apple.ScriptEditor.id.阿里内外`）配置归零，复核用真实已装 App 端到端证明。信任过期/快照的并发覆盖落在 A8 的 `2c0ee31`（同一 `ConfigurationImportValidationTests.swift`） | [`ConfigurationImportValidation.md`](Testing/ConfigurationImportValidation.md) | 3 次负向对照分别 6/5/4 项红；右 Command 侧别位往返保持 `0x0010_0010` 与键码 `[54]` |

共享验证输出（最后一次全量）：`swift test` 退出 0，**392 项 / 34 套**；`scripts/test.sh` 退出 0，`RESULT passed=42 failed=0`；`check-repository-boundaries.sh` 退出 0；`build-app.sh` / `verify-app.sh` / `build-dmg.sh` / `verify-dmg.sh` / `build-driver-dmg.sh` 均退出 0。

**尚未验证（不得表述为已验收）**：10 项修复**全部没有真机验证**。界面离屏渲染复核已于 2026-08-24 完成（App bundle 截图模式，1020×772 中英各 5 张 PNG，所有页面真实文案、无 12pt 以下中文、无遮挡、无溢出、版本号显示完整），但**不等于真实窗口验收**（窗口几何、拖拽缩放、实时交互仍需 GUI 会话）。发布前的真机验收清单见上表各手册；`Release` 本身在真机验收通过前不发。

**已决（版本级别）**：用户已确认保留 `1.8.25-fork.4`（fork 补丁级）并选择了三个关键词「信任到期、语音键、字号」。`Release_Notes_Guidelines.md` 末尾检查清单的「版本号级别与内容相符」项在此放行——fork 补丁序号在本仓库不对应公共 semver 约定，用户知情后已落地，不再视为未通过。


- [ ] 拆分安装制品，修复 pkg 删除已安装 App（待真机验收）
  - `v1.8.25-fork.2` 的 DMG 会因 macOS bundle relocation 把安装目标改写到 Launch Services 记录的旧路径，删除用户正在使用的 `/Applications/Remote Mic.app`，随后 `postinstall` 因目标不存在而失败（Code=112）。用户偏好被新版按全新安装重写。该 DMG 资产已从 Release 撤下并加了警告。
  - 根因：`pkgbuild --root` 未传 `--component-plist`，app bundle 的 `BundleIsRelocatable` 默认为真，而 payload 里同时含 App 与驱动。任何把 App 放在非 `/Applications` 位置的用户都会触发，不限开发机。
  - 修复：按分发形态拆开，让 App 彻底不经过 pkg。`build-dmg.sh` 改为拖拽式 App DMG（App + `Applications` 符号链接，无 pkg、无 BOM、无脚本）；新增 `build-driver-dmg.sh` 出驱动 DMG，内含安装与卸载两个 pkg；驱动组件包移除 App payload，`preinstall`/`postinstall` 删除全部 App 逻辑；`verify-doubao-driver-pkg.sh` 增加"payload 不得含 `./Applications/`、脚本不得引用 `APP_DESTINATION`"反向门禁；`verify-dmg.sh` 改为校验拖拽结构。驱动包体积由 6.8 MB 降至 52 KB。
  - 已完成：247 项 Swift 测试、42 项项目自检、两个 DMG 构建与结构校验、`lsbom` 实测驱动 payload 中 `Applications` 条目为 0、仓库边界与依赖 pin 检查通过。
  - 待完成：真机拖拽安装与驱动安装/卸载验收，尤其 SI-02（存在非标准位置同 Bundle ID App 时拖拽安装不得触发重定位）。手册见 [`Testing/SplitInstallerArtifacts.md`](Testing/SplitInstallerArtifacts.md)，缺陷记录见 [`Bugs/2026-08-21-installer-bundle-relocation-deleted-installed-app.md`](Bugs/2026-08-21-installer-bundle-relocation-deleted-installed-app.md)。

- [ ] 修复遥控器长时间休眠重连后自定义按键映射失效（待真机验收）
  - 现场为 `1.8.17`，`1.8.25-fork.1` 同样存在。遥控器隔夜休眠后重连，写 HID 映射匹配到 0 个服务，`HIDPermissionGate` 门禁不通过，按键监听被永久拒绝，普通按键退回 macOS 原生行为且当天不会自行恢复。语音键不受影响，用户配置也未丢失。
  - 根因：HID event system 注册遥控器服务的时机与 BLE 连接完成相互独立，`matched=0` 是瞬时"尚未就绪"，但被当作终局判定消费，且无任何路径重新评估（权限轮询定时器在启动被拒时不存在，`HID UPDATE RECOVERY` 只在 Sparkle 更新后触发）。
  - 修复：新增纯函数 `HIDMappingRetryPolicy`，在映射开启、已连接、未应用时按 `500/1000/2000/4000/8000/15000` 毫秒退避重跑，超出后维持最后延迟，刻意不设放弃点；只在唯一汇聚点 `startHIDMonitors` 接入，成功归零，`stop`/断连/关闭映射即取消。同时修正 `HID START rejected` 无法区分 `mappingEnabled` 与 `powerKeySuppressed` 的歧义日志。
  - 已完成：247 项 Swift 测试（新增 3 项）、42 项项目自检、Release 构建、仓库边界检查通过；修复前新增测试无法编译，确认测试依赖本修复。
  - 待完成：真机隔夜休眠重连验收。退避上限 15 秒为日志推断值，真实 HID 枚举耗时未测量。测试手册见 [`Testing/HIDMappingReadinessRetry.md`](Testing/HIDMappingReadinessRetry.md)，缺陷记录见 [`Bugs/2026-08-21-remote-reconnect-loses-custom-button-mapping.md`](Bugs/2026-08-21-remote-reconnect-loses-custom-button-mapping.md)。
  - **2026-08-27 更正：本条修复实际上是死代码，从未执行过一次。** 重试判定读了尚未刷新的 `isConnected`，重连时该值为 `false`，于是判定「设备没连着，不用重试」。同一症状在 `1.8.25-fork.5` 再次现场复现，日志中一条 `HID MAPPING RETRY` 都没有。根治见下一条。

- [ ] 打破 HID 接管的循环依赖，让按键接管在重连后自行恢复（待真机验收）
  - 现场为 `1.8.25-fork.5 (124)`，与上一条同症状：长时间闲置重连后全部自定义按键退回原生行为，重启 App 才恢复。
  - 根因两层。一是上一条那个陈旧读取，使唯一的兜底重试从未排程。二是循环依赖：`HIDRemoteMonitor.start()` 要求开关机键已接管才会创建 IOHIDManager 并注册「设备出现」回调，而开关机键能否接管取决于 HID 服务是否已注册——唯一能告知「已注册」的事件，被一道要求它已注册的门挡住了，只剩轮询。此外影响范围本身是错的：一个只涉及开关机键的失败连带禁用了全部按键。
  - 修复：新增 `HIDPermissionGate.canObserve`（不含开关机键接管结果）作为开 manager 的门禁；`deviceDidMatch` 在采纳门禁之前触发新的 `onDeviceAppeared`，由 `retryPowerKeyMappingWrite` 在该时刻**只重跑映射写入**并就地更新已在运行的监听器（不重启任何东西，因此不会重新投递设备、不会重入、不需要 latch）；`process(usages:)` 在未接管前把 power usage 在进入 `activeUsages` 之前就滤除，避免按下走原生而松开被吞；退避到顶后改用「无法安全接管」文案，不再永久显示「仍在接管中」；给 `HIDRemoteMonitor` 补 `deinit { stop() }`；新增计算属性 `hasReadyBluetoothBridge` 作为连接状态唯一定义，修活退避兜底；新增 `connected_power_key_pending` 状态文案。
  - 已完成：404 项 Swift 测试（新增 4 项、1 个 suite）、42 项项目自检、`swift build -c release`、仓库边界检查通过。独立审查 VETO 了初版并全部成立：初版「设备出现后重跑 `applyHIDSettings`」会销毁正在栈上的监听器，那个已死对象继续独占设备且永不释放（全部按键彻底失灵，比原 Bug 更糟），且退避到顶后每 15 秒重复一次。改为 narrow write 后一并消除。另外初版把开关机键的 key-up 吞掉、让永久失败状态不可见、并弱化了一条测试断言，均已改正。
  - 改动一度让 `swift test` 间歇崩溃（EXC_BAD_ACCESS）。从崩溃报告取栈定位为悬空 CGEvent tap（唯一调 `start()` 的测试声明不拥有抑制器却不停它）；处置为该测试改为拥有、`BridgeAppModel` 新增可注入 `hidRuntimePermissions` 并透传给监听器、共享 tap 也受同一探测约束。稳定性实测：改动前 3/3 通过，初版 1/5，最终 8/8。
  - 待完成：真机验收。**最关键的未验证假设**是「`deviceDidMatch` 触发那一刻服务枚举一定 `matched>0`」——整个修复建立在此，且完全未经真机检验；其次是开关机键在待接管窗口内既不执行自定义动作也不休眠（TR-02）。测试手册见 [`Testing/HIDTakeoverRecoveryAfterIdle.md`](Testing/HIDTakeoverRecoveryAfterIdle.md)，缺陷记录见 [`Bugs/2026-08-27-hid-takeover-dead-retry-and-circular-dependency.md`](Bugs/2026-08-27-hid-takeover-dead-retry-and-circular-dependency.md)。

- [ ] 修复连接页出现无法删除的幽灵遥控器卡片（待真机验收）
  - 现场为 `1.8.25-fork.4`，连接页出现两张卡片：真实遥控器「小米蓝牙遥控器 2」与永久存在、无型号无电量的「小米遥控器」。存储中确实是两条持久化档案，第二条 `model=unknown`。语音、按键映射和音频链路均不受影响，但点中幽灵卡片会把当前映射切到那份独立配置，看起来像设置丢失。
  - 根因：落盘入口不止 `.ready` 一处。`remoteProfileID(for:)` 原本"查不到就建"，而它的三个调用方是电量、型号、电源三个纯读数回调，日志证明这些读数先于 `BLE READY` 到达。于是任何通过名称白名单（含通用名 `MI RC`）、连上并回答一次读取的外设都会立刻留下档案，握手从未完成也一样。型号只在读数回调写入，因此这类档案停在 `unknown` 并退回兜底名；界面又没有任何删除设备档案的入口。
  - 修复：`remoteProfileID(for:)` 改为纯查询；三个读数回调在档案尚不存在时按 peripheral identifier 暂存到 `pendingRemoteBatteryLevels`/`pendingRemotePowerStates`/`pendingRemoteModels`；`registerBluetoothBridgeIfNeeded` 成为唯一创建点，落盘后由 `applyPendingRemoteTelemetry` 把暂存值补回；暂存表在桥离开就绪状态时清空，读数回调收到 `nil` 时直接作废对应暂存值。未放宽名称白名单，未改动槽位复用语义，未新增删除设备能力。
  - 已完成：400 项 Swift 测试（新增 4 项、1 个 suite）、42 项项目自检、`swift build -c release`、仓库边界检查通过；三项反向验证各自单独撤掉一处修复，确认只有对应测试变红（其中"查不到就建"的失败信息 `remoteDeviceProfiles.count → 2` 与现场症状同形），并按 sha256 确认源文件逐字节还原。独立审查曾 VETO：暂存机制初版未清空，会把数小时前的电量与型号回放到新建卡片上；该次生缺陷已修复并各有测试。
  - 待完成：真机验收，重点是 GRP-01（全新遥控器首次连接后型号与电量必须正确显示——本次把这两项的写入推迟到握手完成，是最大回归风险）与 GRP-05（量化已知代价：失败外设不再落盘后不会从发现桥候选中"退休"，可能拖慢真实新遥控器首次配对；代码上已确认不会锁死，实际时长未测量，故刻意不加失败退避）。现存的那条幽灵档案不在本次修复范围内，仍会显示。测试手册见 [`Testing/GhostRemoteProfileGate.md`](Testing/GhostRemoteProfileGate.md)，缺陷记录见 [`Bugs/2026-08-25-ghost-remote-profile-from-unready-peripheral.md`](Bugs/2026-08-25-ghost-remote-profile-from-unready-peripheral.md)。

- [x] 同步上游 v1.8.25 并让本 fork 可独立构建
  - 上游 198 个提交已合并；本分支 4 项改动（可配置语音触发键、遥控器麦克风收音开关、右修饰键卡住修复、自定义快捷键修饰键左右侧修复）全部保留，Swift 源码自动合并无冲突。
  - 上游把私有组件 `GetSayAll/sayall-mac-remote` 声明为无条件依赖，本仓库无访问权限，SwiftPM 解析阶段即失败。已改为指向 [`Vendor/sayall-mac-remote`](Vendor/sayall-mac-remote) 本地 stub，`swift build`、`swift test`（244 项）、`scripts/test.sh`（42 项）和 Release 构建均通过。详见 [`Bugs/2026-08-18-private-mac-remote-package-fork-access.md`](Bugs/2026-08-18-private-mac-remote-package-fork-access.md)。
  - stub 使 iPhone、Apple Watch、网页版三条连接路径在本分支构建中不可用；其对话方（iOS App、Watch App、中继服务器）均在私有仓库，无法自行实现。RC003 实体遥控器全部功能不受影响。
  - README 中英文已去除二维码、加群、TestFlight 和官网推广内容，改为说明 fork 血缘、本分支改动与构建方式，并补充 GitHub 标签。
- [ ] 本 fork 的 CI 与二进制发布前置条件
  - `.github/workflows/` 仍会 checkout `sayall-ai`、`sayall-macro-platform`、`sayall-mac-remote` 三个私有仓库，本仓库无访问权限，fork CI 必然失败。需要决定是禁用这些 workflow、改为只跑 stub 构建，还是申请访问权限。
  - [x] Sparkle 更新源与签名身份已完全归属本仓库：`Resources/Info.plist` 与 `scripts/release-variant.sh` 的 feed 指向本仓库，`Sources/RemoteMic/RemoteMicApp.swift` 的 Releases API 与 `Sources/RemoteMic/AppLinks.swift` 的 GitHub 链接一并改为本仓库；`SUEnableAutomaticChecks` 已改为开启，`SUPublicEDKey` 已换成本 fork 新生成的 EdDSA 公钥 `+EyNzAtTgwbJ4/04/ujn/JrpA0NKLFQSOd9w3Pg80M8=`。`scripts/verify-app.sh` 的断言同步更新：自动检查断言为 `true`，并新增“App 内公钥必须等于源 `Resources/Info.plist`”“不得为上游公钥”两条。
  - **一次性升级中断（必须写进发布说明）**：上游 Sparkle 私钥不在本仓库手上，换用新公钥后，已经装了 `1.8.25-fork.1` ~ `1.8.25-fork.3` 的用户会拒绝一切用新私钥签名的更新——签名验签失败，Sparkle 不会提示、也不会安装。**`1.8.25-fork.4` 必须手动下载安装一次**；从 fork.4 起自动更新才真正生效。
  - **新私钥保存在登录钥匙串（account `ed25519`），必须离线备份**（`generate_keys -x <file>` 导出）。钥匙串丢失或被重置后无法再签名任何更新，只能再换一次公钥并再让全部用户手动安装一次。
  - [x] ad-hoc 已成为显式的一等发布模式，而不是被绕过的门禁：新增 `scripts/release-signing-mode.sh`，`RELEASE_SIGNING_MODE` 默认 `developer-id`，`adhoc` 必须显式开启。`adhoc` 只豁免 Developer ID 授权/Team 断言、Apple 公证/staple/spctl、上游 Team ID 相等门禁，以及本仓库无法访问的生产服务与私有包标记；同时新增 ad-hoc 专属断言（签名必须存在、内部有效且确实为 ad-hoc，Developer ID 产物走 ad-hoc 路径会被拒绝）和 appcast 签名断言（签名形状合法、签名密钥公钥必须等于 App 内 `SUPublicEDKey`）。无法证明的项由 `release_signing_mode_report` 在发布时逐条打印。
  - Gatekeeper 拦截仍然存在且已确定不修：没有 Apple Developer ID 证书，只能 ad-hoc 签名，首次打开需右键“打开”。这是既定策略，见 `AGENTS.md`「macOS 预览候选分支」一节。
  - 仍未解决的上游假设：`scripts/publish-release.sh` 的公开验证会下载 `https://download.sayall.app/mac/releases/<tag>/` 并校验 Cloudflare 响应头与稳定版跳转，本 fork 没有该 CDN，因此非 dry-run 的 `prerelease` 会在公开资产比对阶段失败；`scripts/fast-release.sh` 与 `scripts/package-macos-release-in-actions.sh` 依赖上游私有的 `remotemic-notary-secrets`、`apple-signing-match` 和 `Apps/MobileWeb/.private/production.env`（后者的路径已被 `scripts/check-repository-boundaries.sh` 禁止存在），在本仓库无法运行。`.github/workflows/mac-release-package.yml` 的两个私有凭据仓库刻意保留上游路径，不伪造 fork 替代。
  - 测试手册见 [`Testing/ForkSparkleIdentityAndAdHocRelease.md`](Testing/ForkSparkleIdentityAndAdHocRelease.md)。自动化用例 1–2 与代理用例 3–4 已通过；**用例 5（`fork.3 → fork.4` 一次性中断）和用例 6（`fork.4` 之后自动更新可用）尚未真机验收**——用例 6 还需要一个 `fork.4` 之后的版本才有条件执行。稳定功能回归项也未在真机完成。
  - Bundle ID 仍为上游的 `com.hd838a.RemoteMic`，本分支构建与上游安装包会互相覆盖。若要与上游共存需改 Bundle ID，代价是蓝牙、输入监控、辅助功能权限全部重新授权。

- [x] 统一 SayAll 品牌、官网与当前上架战略
  - App 英文名称统一为 `SayAll`，中文名称继续使用“无线麦”，官网统一为 `https://sayall.app`。
  - Mac App 继续采用独立下载方式维护，Mac App Store 上架暂时暂停；当前只推进 iOS App 与内嵌 Apple Watch App 上架，且不拆分商店专用版本。

- [x] 使用 Cloudflare CDN 加速 Mac 下载
  - 官网固定入口为 `https://download.sayall.app/mac`，只解析 GitHub 最新正式版并跳转到同域名的版本化 DMG，不把 Pre-release 作为默认下载。
  - Sparkle 的稳定 feed 和预发布发现继续使用 GitHub，版本化 ZIP 与本地化更新说明改走 `download.sayall.app/mac/releases/<tag>/`；旧安装用户无需迁移 feed。
  - GitHub Releases 继续保存全部签名、公证资产；发布后必须从 GitHub 与 CDN 分别下载并逐字节比较，同时验证 `HEAD`、`Range`、签名、公证和候选更新发现。完成公开 Worker、官网和 `1.8.12` Pre-release 验证后再勾选。
  - 2026-08-12：Worker 与中英文官网已部署，当前正式版 `v1.8.3` 的 GitHub/CDN DMG 字节一致；候选 appcast、签名公证资产和正式版到候选更新仍由统一 Mac 预览版流程完成。

- [ ] 在 Mac App 提供低门槛问题反馈入口
  - 状态栏菜单已增加“问题反馈”，点击后由默认浏览器打开 SayAll 工作台的最低权限入口；不传递账号、设备标识、Token 或动态口令。
  - 自动化已覆盖固定入口地址；真实浏览器跳转、自动进入排行榜及提交反馈仍需人工验收，通过后再标记完成。
- [x] 建立可选硬件信号模拟回归门禁
  - 私有模拟器 V1 已覆盖小米遥控器 12 个原始按键、36 个短按/双击/长按场景、7 个长按连发场景，以及 BLE 分片、sync、异常、重连、旧回调和双设备隔离；模拟依赖仅在测试环境变量存在时加载，普通开源构建不依赖私有仓库。
  - 自动化只代表 L0–L2 进程内协议和状态响应通过；CoreBluetooth、IOHID/DriverKit、Power 系统行为、射频、音质和安装环境继续由系统级与 RC001/RC003 真机门禁负责。

- [x] 建立可选私有功能组件集成边界
  - 私有功能的源码、测试、资源和内部文档由独立私有组件维护；本仓库只保留稳定的可选适配层和语音会话生命周期契约。
  - 未提供私有组件时，公开源码仍可完整构建且不展示私有入口；正式发布流程必须显式提供并验证私有组件，禁止静默生成缺少私有能力的安装包。
  - 详细功能规划、资格管理和服务配置不再保存在公开仓库。
  - 2026-08-14：修复快捷指令组件在最终 macOS App 中错误使用 SwiftPM 根目录资源路径的问题；宿主继续把私有组件 Bundle 放在 `Contents/Resources`，最终 ZIP 验证必须移除同版本构建缓存后真实打开受影响页面，避免绝对构建路径回退掩盖打包缺陷。
  - 公开宿主不提供私有功能资格入口，也不会主动创建相关身份或发起资格请求；未提供私有组件时继续完全隐藏，已授权用户的本地功能开关仍默认关闭。

- [ ] Windows 版本
  - 当前范围只包含小米 RC003；已完成相关 forks、Windows 实现、权限/安装和虚拟麦克风路线研究。首选源码基线为 `miaomiaozii/windows-remote-mic-app`，但产品化仍需独立真机验收、免费自签流程和第三方组件审查。
  - 已在主仓库发布独立的 [Windows RC003 Community Preview v0.1.0](https://github.com/HD838A/remote-mic-app/releases/tag/windows-v0.1.0-community-preview)，镜像贡献者提交 `271ed794` 的未签名安装器、便携版、对应源码快照和 SHA-256 清单。该版本仅作为社区预览，不代表 PR #3 当前 HEAD 或正式产品化完成，Windows 源码仍未合入 `main`。
  - Windows 与 macOS 必须独立构建、签名、打包、验证和发布：Windows 当前决定只使用免费自签 Authenticode 证书与 EXE/MSI/ZIP 产物，不购买微软/公共 CA 签名，也不复用 Apple Developer ID、APP/PKG/DMG、公证或 Sparkle 流水线。自签版本仍会出现未知发布者或 SmartScreen 提示。
  - Windows 可行性、候选源码、Release、CI、许可与最终排名研究材料已迁移至独立的产品资料工作区，不随公开源码仓库发布。
- [x] 开发手机伴侣 App，让手机可以替代或补充蓝牙遥控器控制 Mac
  - 已实际完成并运行良好。Mac 端负责执行按键动作、打开应用和输出虚拟麦克风；手机端提供虚拟遥控按键，以及点按切换或按住说话两种语音方式，不要求用户必须购买 RC003。
  - iOS 16+ SwiftUI 工程现由独立私有仓库维护，并已完成核心遥控页面：方向、确定、返回、主页、菜单、TV、音量、关机和语音均具备按压状态、无障碍标签与增强震动反馈；麦克风键既支持短点按开始、再次点按停止，也保留按住说话、松手停止，录音真正就绪时追加一次清晰反馈；已授权时预热并复用录音管线，语音开始信号先于音频引擎启动发送，以缩短首句等待。iOS 17 及以上继续使用当前麦克风权限与状态监听 API，iOS 16 单独使用系统兼容分支。全部 12 枚可配置按键（含关机键和两个确定入口）会向支持该能力的 Mac 发送按下/松开事件，复用实体遥控器的单击、双击和长按识别；旧版 Mac 自动回退到原单击命令。顶部移除 Logo，设备连接状态、名称或校验码居中显示；六枚中部按键按“返回 / 菜单 / 音量+、主页 / TV / 音量-”排列并向中心微收，Mac 端存在非默认按键配置时会在全部 12 枚对应可配置按钮内部同步显示简短动作标题；顶部关机键的长标题可完整换行显示，六枚中部按键使用统一的图标区和标题区高度，使菜单等不同形状图标下方的文字保持同一水平基线且不越界。页面使用仓库内高分辨率浅银色铝合金拉丝背景，麦克风按钮采用与金属机身协调的低饱和钛灰配色，固定单屏布局且不支持滚动。
  - 已接入附近直连最小闭环：iOS 通过 Bonjour 发现 Mac 并显示真实设备名称；浏览器只有在本地网络权限链路进入 `ready` 后才建立连接，等待授权期间不提前连接。每次全新发现优先使用普通局域网，3 秒无结果后切换点对点模式；点对点模式只做两次有限重建，两种模式均无结果时会提示用户重新开关 Wi-Fi，仍失败再重启 iPhone。发现、连接、授权和权限等待状态不会被降级或重试打断，手动刷新及明确失败后的前台恢复都会重新从普通局域网开始。iOS 进入校验码显示状态并回复准备完成后，Mac 才显示授权弹窗；断开的待授权连接会自动撤销，新的已认证连接可安全接管残留的已授权旧会话，重新打开 iOS App 不再需要重启 Mac。Mac 弹窗和 iOS 顶部都使用醒目的大号校验码。Network 底层错误只保留在开发日志中；iOS 会在沙盒内轮转记录 App/系统版本、生命周期、网络路径与接口、Bonjour 发现模式、浏览代次和结果增删、模式切换与重试计划、连接路径与耗时、安全会话阶段及麦克风启动阶段，用户可主动分享 `.log` 脱敏日志，日志不包含音频、按键内容、设备名称、IP、校验码、密钥或身份信息。Mac `1.6.7` 返回的辅助功能/按键失败和语音输出失败会转换为可操作的中文提示，未知服务端错误仍使用通用提示。麦克风权限只在首次使用语音按钮时触发，录音会话使用与纯录音匹配的系统模式，授权后可点按或按住录音。
  - 附近连接已接入会话级安全保护和双方校验码。用户首次在 Mac 允许后，该次 iOS App 安装会成为受信任设备，后续附近连接自动授权；删除 iOS App、重新安装，或在 Mac 设置中清除受信任设备后，都必须重新确认。当前手机伴侣 App 的附近直连、遥控和语音闭环已经满足完成标准；跨互联网控制属于可选后续能力，不影响本条完成状态。
  - TestFlight 发布链路已跑通：iOS 工程使用 Automatic Signing，Xcode Cloud 已成功上传内部 TestFlight，且已完成真机安装体验。公开文档不记录开发者团队或应用标识。首次使用语音按钮会主动请求麦克风权限，即使 Mac 尚未连接也不会静默忽略授权入口。
  - iOS 核心回归测试已覆盖固定六键顺序与图标、全部可配置按键的白名单协议名称、重要按键震动强度、语音松开反馈、前后台重连判断、普通局域网到点对点的有限降级策略、用户可见错误转换、两位校验码格式，以及遥控协议消息的完整编解码和旧版缺省字段兼容。
  - iOS `0.8.8` 完善 Mac App 信息页：进入页面时立即重新发现 Mac，并在与遥控页一致的铝质背景上展示 Mac 名称、版本、连接方式、配对状态、最近连接和连接检查；同时显示官网下载入口、实体麦克风遥控器推荐、语音本地传输说明，并支持系统分享（包括 AirDrop）和复制链接。页面标题使用“无线麦”，顶部导航及 54pt 左右按钮保持固定，信息模块支持滚动；页面底部显示当前 iOS App 的版本与 Build，点击可复制，并提供分享脱敏诊断日志入口。页面外边距和卡片间距统一为 20pt，信息卡片保持 14pt 圆角和一致的内部留白；返回按钮和左侧边缘右滑均可返回遥控页。下载卡片的大号地址直接显示带 `https://` 的官网地址并打开官网，官网、分享和复制链接跟随 App 当前语言。iOS App 已支持中文和英文，首次安装默认使用手机语言，第二页底部可随时切换并持久保存选择；语言切换上方以清晰说明显示 TestFlight 公测完整链接，点击即可复制。新版 Mac 会把真实 App 版本发送给 iOS，旧版 Mac 仍可兼容连接并回退显示运行状态。有刘海或灵动岛的 iPhone 会将不透明的“App Logo + 无线麦”彩蛋绘制在顶部切口后方，彩蛋不参与页面布局；Logo 复用主屏 App Logo，并使用同一 App Icon 圆角矩形轮廓。
  - 第一阶段优先支持 iPhone/iPad 前台使用：通过系统网络框架和附近发现服务安全连接 Mac。附近设备可以使用点对点链路，不应把“连接同一个无线路由器”设为硬性前提；长期设备信任已经落地，并支持在 Mac 端主动清除。
  - 手机遥控仅作为体验与备用功能：Mac App 启动时不自动开启附近连接服务；用户每次启动后必须主动点击“连接手机”才开始等待 iPhone，等待期间可以随时取消并释放监听与旧会话，再次开启后可由其他 iPhone 连接。等待状态使用提醒色，“无需邀请码”使用中性色，不再误用连接成功绿色。
  - 按键协议当前只发送白名单虚拟按键单击事件，由 Mac 根据本机配置映射到现有 `ButtonAction` / `KeyboardInjector`；禁止手机直接下发 shell、任意脚本或未授权键盘数据。按住重复、双击和长按手势留待后续真机调节。
  - 手机麦克风第一版已发送 16kHz 单声道 PCM，并由 Mac 接入现有 `VirtualAudioOutput` 和 MiRemoteV 2ch 链路；手机语音会话开始和结束时，Mac 会同步产生一次系统语音键按下和释放，使手机音频能够进入与实体遥控器一致的豆包唤起流程。后续再根据弱网和延迟实测决定是否增加 Opus、抖动缓冲和音频中断恢复。第一版保持 App 在前台，不承诺锁屏遥控、后台常驻或持续收音。
  - 后续再评估 Android、桌面小组件/快捷指令、多台 Mac 切换和跨互联网远程控制；跨互联网需要独立的账号、端到端加密、设备撤销和中继服务，不与附近直连第一版混在一起。
  - 架构选择、官方平台限制、实现阶段和验收研究材料已迁移至独立的产品资料工作区，不随公开源码仓库发布。
  - iOS 源码、专属 CI、设计备选图和 iOS 待办已迁移到独立私有仓库；本仓库继续维护 Mac 端附近连接、授权、按键执行和音频接收，拆仓不改变现有协议或运行行为。Mac CI 已增加仓库边界门禁，禁止迁出的 iOS 目录和专属 CI 回流。

- [ ] 为 Apple Watch 提供独立的 Mac 连接入口 <!-- workshop:status=待验收;priority=P2 -->
  - 连接页已按 iPhone → Apple Watch → 网页版的顺序加入专用入口；iPhone 与 Watch 共用同一个按需开启状态，任一入口都可以取消等待，Mac 启动时仍不自动监听。
  - 2026-08-14 修复 `1.8.22` 实际未启动 Watch BLE、旧 TCP 取消回调误停 Watch BLE，以及 Mac Bonjour 只监听端口但未确认服务发布的问题。Mac 现在只在用户点击后同时启动 Phone Bonjour 与 Watch BLE；Bonjour 发布超时、被移除或 listener 失败会自动恢复，Watch 等待 GATT 服务注册完成后才广播。
  - 2026-08-15 根据真机日志修复 Watch BLE 音频积压：Watch 端控制优先并限制约 300ms 实时窗口；Mac 端分别跟踪 iPhone 与 Watch 语音来源，通道占用和输出未就绪返回不同普通用户提示，延迟停止不再释放或污染另一端会话。
  - 2026-08-18 补充首次收音业务就绪握手：新版 Mac 只有在虚拟麦克风与系统语音键准备成功后才发送可选 `voiceReadyV1`，新版 Watch 收到后才启动采集；停止、断线或失败会取消迟到开始结果，旧版两端继续走原兼容路径。
  - 2026-08-18 针对真机“Watch 已开始收音但第三方工具没有有效电平”，修正 Mac CoreBluetooth 批量写请求只响应一次；组件与宿主增加 GATT 接收、重组、解码、peak/RMS、来源门禁和虚拟输出入队分层日志。配套 Watch 将无响应写入限制为 161-byte 安全包，同时保留系统背压和完整音频，不以丢帧换取低延迟。
  - 2026-08-15 补充分离的 iPhone / Watch 已连接状态：成功授权后对应行显示“已连接”和“取消连接”，断线但监听仍开启时回到等待，取消连接会断开当前会话并同时停止附近监听；等待状态继续使用提醒色，只有真实连接使用成功色。
  - Mac 附近连接与网页版会话实现已迁入独立 `SayAllMacRemote` 组件并固定 revision；Watch 授权提示会根据设备名称显示专用说明，既有 iPhone、Web、实体遥控器和按键映射链路保持兼容。本机 Release APP 已验证 Bonjour 可被系统发现、Watch BLE 已广播且取消等待会停止两者；当前 Mac 226 项、私有组件 24 项回归通过。
  - 自动化与 Release 构建通过后仍需真实 Apple Watch、实际测试 Mac、虚拟麦克风和第三方语音工具完成发现、授权、按键、收音、取消等待及客户端接管验收，再标记完成。
- [x] 开发静态产品信息网页
  - 展示无线麦的核心定位、主要功能、适用场景、下载入口和基础使用方式，并提供清晰的产品截图与录屏演示。
  - 页面应适配桌面和手机浏览器，优先保证加载速度、素材清晰度和分享体验；截图、录屏、版本信息和下载链接需要便于后续持续更新。
  - 本条只负责产品信息与媒体素材展示，与下方可实际连接并控制 Mac 的“手机网页版遥控器”是两个独立功能。
  - 中英文纯静态官网已完成并部署到 `sayall.app`，包含下载入口、核心功能、使用步骤、真实产品截图、系统要求与隐私说明；旧域名继续重定向到新官网。当前尚未加入真实录屏演示，因此本条暂不标记完成。
- [x] 发布前完成品牌、域名与知识产权清单核对
  - 在正式更换产品品牌或对外发布前，统一核对品牌写法、官网与下载入口、App 名称、应用标识、公开账号、商标初筛、Logo 权属、第三方许可和必要的发布联系信息，避免不同渠道使用相互冲突的名称或资产。
  - 公开仓库只保留这项发布门禁，不记录域名持有人、账号状态、商标检索判断、邮箱规划或其他品牌资产明细；完整品牌与知识产权清单保存在私有产品资料库。
- [x] 开发无需安装的手机网页版遥控器
  - 网页版作为原生 iOS Nearby 连接之外的独立入口：Mac 主动连接公网 WSS 会话中继，手机通过 HTTPS 页面扫码加入，经 Mac 校验和明确允许后发送白名单按键与按住说话音频。
  - 第一版不需要账号、局域网发现或路由器端口转发；使用临时会话、传输加密、严格消息限制和不保存语音内容的隐私边界。WSS 不能描述为服务器不可见的端到端加密，后续再根据技术需求评估 WebRTC 或应用层端到端加密。
  - 真实域名、VPS、IP、SSH、证书、Token 和生产配置不得进入开源仓库；公开代码只保留占位符和脱敏模板。
  - 第一版代码与公网部署已完成：包含 React 手机单屏遥控器、内存 WebSocket relay、Mac 扫码与逐次批准、白名单按键、自定义标题、16kHz PCM 按住说话、移动语音来源互斥、限流、安全响应头和隐私日志边界。
  - 全部 12 枚可配置按键已增加可选按下/松开事件能力，Web 与 Nearby iOS 分别维护手势状态，并复用实体遥控器的单击、双击和长按配置；连接旧版 Mac 时自动回退到原单击命令。Web 前端与中继服务已分别迁移到独立私有仓库，并各自使用专属 GitHub CI；本仓库只保留 Mac 客户端的协议实现、连接入口和发布配置。
  - 第二轮弱网修复保持现有 WSS 架构，暂不引入 WebRTC：扫码页面只解析并暂存一次性会话凭证，必须由用户点击“连接 Mac”后才建立 WebSocket；麦克风仍只在按住说话时请求。已加入手机端 60 秒短时断线恢复、Relay 已批准会话续接、音频发送积压保护、浏览器自动增益/降噪、Mac 端 240ms 起播抖动缓冲，以及播放完尾音后再释放系统语音键的收尾流程。
  - 网页版视觉已独立适配 Safari：使用与浏览器栏融合的纯色背景，顶部只显示连接状态和 Mac 名称；中间六键使用与方向盘一致的石墨色圆角矩形，麦克风和确定按钮支持从按钮内部开始纵向滑动。页面禁止长按选择文字，并在支持的平台提供按键震动；控制器使用稳定视口高度避免 Safari 地址栏收放导致滚动抖动，页面下方保留约 25KB 的内联 App Logo，并提供 Mac App 下载、单一官网、iOS TestFlight 公测、复制公测链接、实体遥控器推荐和连接教程。README、Mac 关于页与菜单、iOS Mac App 信息页也已补充官网入口。已批准会话在两小时内可免重复扫码，但恢复连接仍必须由用户手动点击。
  - Mac 端打开手机网页版连接前需要输入固定邀请码 `8586`；验证只在当前 App 会话内有效，不写入配置，未验证时不会启用连接或显示二维码。邀请码弹窗顶部优先推荐 iOS App，并提供集中配置的 TestFlight 公测入口及复制链接操作，公测地址同时写入中英文 README。
  - iOS TestFlight 公测不需要网页版邀请码。正式 Mac 发布流程会从本机私有生产配置注入 WSS 地址，并在构建和最终 App 校验阶段强制检查；地址缺失时不再打开内部会话页，连接页直接显示“正式服务尚未配置”。
  - 当前已通过 TypeScript 检查、生产构建、18 项 Web/relay/浏览器测试、129 项 Swift Testing、42 项 Self Test 和 Release App 构建；公网 HTTPS/WSS 已再次完成建会、网页加载、加入、批准、按键、语音握手、二进制音频和关闭全链路验收。
  - iPhone 17 Pro Safari 已完成核心麦克风真机体验，语音链路可用，存在但可接受的延迟；尚需继续验证虚拟麦克风实际听感、切后台停止、Android Chrome、不同网络组合与现场延迟，完成这些人工验收后再勾选本条。
  - Web 前端、服务端及其详细开发资料由独立私有仓库维护；共享协议变更必须与 Mac 客户端保持向后兼容并完成跨仓库验证。Mac CI 已增加仓库边界门禁，禁止迁出的 Web/Server 目录和专属 CI 回流。
- [ ] 本地统计增加用户最常用的快捷键和实体按键排行
  - 在现有“统计”页面按“日 / 周 / 全部”展示最常使用的已配置快捷键或动作，以及 RC003、iOS 和网页版遥控器实体按键排行；数据仅保存在本机，不上传按键内容、快捷键配置或使用明细。
- [ ] 在左侧侧边栏新增“排行榜”，展示所有主动参与用户的全局使用排名
  - 已先将现有纯本地统计从“关于”页迁移为独立“统计”页：左侧使用醒目的大号“日 / 周 / 全部”切换，按日显示最近 7 天柱状图；按周显示最近 7 周，“更早”只汇总确实带日期且早于最近七周的记录。旧版仅有累计值、缺少日期元数据的部分只保留在“全部”，不再推算到日、周或“更早”；页面同时显示仅保存在本机的单次语音时长 Top 10。本阶段不包含全局排行榜、数据上传或服务端能力，本条仍未完成。
  - 新产生的本地每日统计使用版本化元数据，同时保存输入来源、具体按键或语音入口、小时分布、语音会话次数、各来源语音时长、最长会话、首末活动时间、时区和日历来源；单次语音 Top 10 保存开始时间、结束时间和来源。元数据保持本地、按日聚合且不记录设备标识、按键映射、快捷键内容、目标 App、文本或音频。
  - 当前本地统计已有今日、本周和累计三个维度的按键次数与语音时长；排行榜第一版提供按键次数、最长单次语音时长和累计语音总时长三个指标的日榜、周榜与总榜，显示 Top N、当前用户排名、差距及最近同步时间，不上传原始语音、按键内容、快捷键配置或应用使用明细。
  - 已观察到用户单次连续语音输入可达到约 3 分钟，因此单次时长不能按几十秒或 1 分钟简单截断；以一次真实的语音开始到停止作为单次会话，界面统一使用时分秒显示，并分别记录“今日最长单次”“本周最长单次”“历史最长单次”“今日累计”“本周累计”和“历史累计”。本地每日统计已包含当日按键次数，并按本机日历聚合、将跨日语音计入结束当天；服务端日榜仍需明确统一的结算时区和跨日会话归属规则。
  - 排行榜必须默认关闭并由用户主动加入；加入前清楚说明会上传哪些聚合数字。未加入的用户继续保持现有纯本地统计，不出现在榜单中，也不因打开排行榜页面自动上传数据。
  - 用户使用随机生成的排行榜身份和自选昵称，不以蓝牙地址、外设 UUID、Mac 序列号、Apple ID 或其他硬件标识作为公开或服务端主键；设备凭证存入 Keychain，支持退出排行榜、删除云端数据和重新生成身份。
  - 为减少历史数据篡改和刷榜，排名默认从用户加入排行榜后开始累计，不直接信任或一次性上传既有本地总数；客户端按增量批次同步，服务端校验累计值单调递增、单次会话起止时间与持续时长一致、合理速率、重复请求和版本签名，并提供限流、封禁与榜单重算机制。真实的数分钟长语音不能仅因时长较长就判定为作弊。
  - 需要新增独立后端 API、数据库、定时日榜与周榜结算、匿名身份认证、离线增量队列、失败重试和服务状态降级。无网络或服务不可用时仍显示本地统计，并明确标注排行榜暂不可用，不能影响蓝牙、语音和按键核心功能。
  - 实现时同步更新“关于”页、README 和隐私说明中“数据仅保存在本机 / 不会上传”的现有表述；发布前明确数据保留期限、删除流程、服务器区域、运营成本和隐私政策，并完成接口滥用、重放、伪造统计及昵称内容审核测试。
  - 工作量判断：中等偏多。侧边栏和榜单 UI 较小，主要成本是可信统计、服务端长期维护、隐私合规和防刷；若不准备运营后端，可先做仅本机的个人成就页，不能称为全局排行榜。
- [ ] 基于本地语音转写历史主动生成“昨日回忆”“夸夸自己”等每日回顾 <!-- workshop:status=待处理;priority=P2 -->
  - 当前公开 TODO 没有同类条目；私有产品战略已经规划“语音历史进入本地知识库，并生成每日或每周回顾”，本条是该方向面向用户的明确产品入口。
  - 前置依赖是用户明确选择保存的本地语音转写历史和稳定的记录时间、来源及原文引用。当前 Mac App 只保存语音次数、时长、来源和时间段等聚合统计，不保存日常转写正文；不能仅凭这些数字生成有内容的回忆，也不能把 Onboarding 中临时出现的测试文字当成历史记录。
  - 每日回顾优先整理昨日的重要想法、决定、待办、反复出现的主题和未完成问题，并允许从每条结论返回对应原始记录；资料不足时明确显示“昨日没有足够记录”，不得补写或猜测用户没有说过的内容。
  - 增加可选的“夸夸自己”视角：从昨日真实记录中提炼已经完成的事情、坚持投入的行动、解决的问题和可以被看见的进步，用具体事实给出正向反馈。不得脱离原始记录进行空泛吹捧、虚构成果或对用户性格和心理状态作未经依据的判断；用户可以查看对应来源，也可以关闭这类内容。
  - 语义整理需要接入用户选择的大模型 API 或后续官方托管模型，但只发送用户明确允许参与本次回顾的文字范围。生成结果默认保存在本地，支持编辑、重新生成、导出和删除；功能默认关闭，不因存在语音时长统计就自动上传内容或推送回顾。
  - 产品价值判断：当本地转写历史已经可用时价值中高，它能把被动历史变成每天可感知的持续收益，并自然连接待办和知识库；但当前 ROI 受前置数据层制约，应先完成可搜索、可追溯、可删除的本地转写历史，再开发自动回顾，不能排在语音输入核心质量之前。
  - 详细的数据模型、模型调用、知识库和商业化边界继续保存在私有产品资料库；公开仓库只记录用户行为、依赖关系、隐私边界和验证状态。
- [x] 将最低支持版本降低到 macOS 14.0，仅支持 Apple Silicon Mac
  - macOS 14.0、14.4 和 15.0 的 `arm64` 编译探测结果完全一致：66 条可用性诊断全部集中在 `SettingsView.swift` 的 macOS 26 Liquid Glass API，没有额外核心功能编译错误。
  - MiRemoteV 2ch 已成功构建为 `arm64`、Mach-O `minos 14.0`；Sparkle 2.9.4 的 Apple Silicon Framework 最低版本为 macOS 11.0。
  - macOS 26 继续使用原生 Liquid Glass；macOS 14/15 使用非 Glass 系统兼容样式，保持功能和布局可用，不要求视觉完全一致。
  - 发布产物继续只包含 `arm64`，不支持 Intel、`x86_64` 或 Universal Binary；同步调整并验证 App、MiRemoteV 2ch、PKG、DMG、Sparkle 更新和公证流程。
  - 代码与发布机验证已完成：macOS 14.0 App 编译、36 项 Self Test、76 项 Swift Testing、App/驱动 `minos 14.0`、两个 PKG 和 DMG 的结构及权限验证均通过。
  - 发布机运行时审计发现并修复了本地化订阅触发菜单重建时复用状态项被同时插入两个 `NSMenu` 的 AppKit 断言；启动和语言切换必须保持无该异常。
  - v1.4.12 以 pre-release 发布，v1.4.13 已晋升正式版；最低版本、架构、签名、公证和 Sparkle 元数据均已进入正式发布链。
  - macOS 14 完整研究、实现边界和验收材料已迁移至独立的产品资料工作区，不随公开源码仓库发布。
- [x] 在真实 macOS 14 Apple Silicon 机器完成运行时验收
  - 已在真实 macOS 14 Apple Silicon 机器完成运行时验收，确认实际运行正常。
  - 验收范围包括安装与卸载、降级界面、蓝牙配对与重连、HID、权限、ATVV 音频、MiRemoteV 2ch、Sparkle 更新、睡眠唤醒和音频路由变化；详细依据保存在独立的产品资料工作区。
- [x] 支持 2017 Intel MacBook / MacBook Pro 的 macOS Ventura 13
  - 已完成 macOS 13 SwiftUI 可用性适配，并建立与 Apple Silicon 正式发行线隔离的长期 Intel 发行变体：Intel 仅构建 `x86_64`，输出到 `dist/intel`，使用带 `Intel` 的独立资产名和 `appcast-intel.xml`；默认发行仍保持 macOS 14、`arm64`、原资产名和 `appcast.xml`。
  - Intel App、Sparkle 五个可执行文件、MiRemoteV 2ch、安装/卸载 PKG 和 DMG 已通过构建与静态验证；安装器会在删除已有 App 前检查系统与架构，普通用户机器上的脚本不依赖 Xcode 或 Command Line Tools。
  - 预览版 Feed 解析已按当前稳定 Feed 的文件名选择资产，Intel 开启预览版更新时不会串到 Apple Silicon 的 `appcast.xml`。Apple Silicon 默认构建和发行产物仍需作为每次 Intel 改动的回归门禁。
  - 已经过多名 Intel Ventura 用户测试，安装、启动、蓝牙遥控、按键和语音核心路径可以正常使用，Intel 发行线转为正式支持。公开测试步骤见 `Testing/IntelVenturaCompatibility.md`。
  - GitHub Actions 对 Apple Silicon 与 Intel 分别构建和验证；正式候选通过受保护的 `mac-release` Environment 生成两套独立签名、公证产物。稳定晋升沿用同一批候选字节，不重新打包。
- [ ] 优化 macOS 安装器，为普通用户只提供一个明确的安装入口 <!-- workshop:status=已规划;priority=P1 -->
  - 当前 DMG 同时展示 `Remote Mic.app`、Applications 快捷方式、`Install Remote Mic.pkg` 和卸载 PKG，用户容易误以为 App 与 PKG 需要分别安装，或不知道应该选择哪一种。现有安装 PKG 实际已经同时安装 App 和 `MiRemoteV 2ch` 兼容麦克风，主要问题是发布入口重复而不是缺少合并安装能力。
  - 第一方案是让 DMG 只突出一个签名并公证的 `Install Remote Mic.pkg`，一次完成 App、兼容麦克风、权限正确性检查和安装后启动；不再在同一 DMG 根目录提供并列的 App 拖拽入口。只需要 App、已经安装其他回环设备的高级用户可通过单独的 ZIP 或明确的高级下载入口获取，不与普通安装流程并列。
  - 不优先把安装器包装成自定义 `Install.app`：音频驱动仍然需要管理员授权和系统安装能力，包装 App 最终仍需调用内嵌 PKG 或增加特权辅助进程，会扩大嵌套签名、公证、权限、失败恢复和安全验证范围。只有系统 PKG 的交互无法满足品牌引导或安装状态展示时，才进一步评估 `Install.app` 外壳。
  - 卸载入口不与首次安装争夺注意力：优先在 App 的帮助或关于页面提供“卸载兼容麦克风”及说明，必要时再下载或打开已签名的卸载 PKG；卸载必须明确区分“只移除兼容麦克风”和“同时删除 App 与本地设置”。
  - 需要验证全新安装、已有 App 升级、已有驱动升级、只装过 App、只装过驱动、安装取消、管理员授权失败、安装后首次启动、Sparkle 后续更新和完整卸载；新的单入口流程必须继续通过 App、PKG、DMG、签名、公证、Gatekeeper、文件权限和最终安装结果校验。
  - 2026-08-13 已完成候选代码：DMG 根目录只保留一个安装 PKG；App-only ZIP 和卸载 PKG 继续作为高级 Release 资产。驱动在 PKG 内暂存，安装后使用系统自带 `file`、`plutil`、`codesign` 和文件权限检查判断现有 `MiRemoteV 2ch` 是否健康且同版本、同架构，健康时原样保留，缺失、损坏、架构不符、签名异常或版本不匹配时才替换；安装脚本不调用开发者工具。本轮按要求未生成产物，完成真实安装矩阵后再勾选。
  - 2026-08-15 已为 Apple Silicon 与 Intel 独立安装包增加系统 Installer Distribution 门禁：使用真实硬件能力而非 `uname -m` 判断架构，中英文界面会在安装前明确提示错误包并指向另一版本；preinstall/postinstall 继续保留二次检查。双变体 ad-hoc PKG/DMG、产品归档结构及 Apple Silicon 拒绝 Intel 包的只读命令行路径已验证；最终 Developer ID 签名、公证包仍需在真实 Intel 与 Apple Silicon 上完成 Installer.app 双语言交叉验收后再勾选。
  - 2026-08-16 已精简未来公开 Release：两套安装 PKG 继续完整保留并验证在对应 DMG 内，但不再作为 standalone 资产重复上传；两架构共享中英文更新说明并合并 DMG SHA-256 清单，公开矩阵由 17 项降为 12 项。历史 Release URL 和 15/17 项晋升兼容保持不变；下一份真实签名候选仍需验证 GitHub/CDN 12/12 字节、两架构安装与卸载。
- [x] 设计并实现完整的首次 Onboarding、权限设置流程，并重新布局“连接”和“按键映射”页面 <!-- workshop:status=已完成;priority=P1 -->
  - 首次启动使用可恢复、不可跳过的多步骤 Onboarding，而不是一次弹出全部权限或只展示功能介绍。当前流程依次覆盖：欢迎、选择语音工具、蓝牙/输入监控/辅助功能权限、遥控器连接与实体按键、音频回环设备选择、真实语音文字测试、三个普通遥控器按键和完成页。
  - [ ] 在 Onboarding 第一页增加遥控器蓝牙配对提示：同时长按“主页 Home + 菜单 Menu”可使遥控器进入蓝牙配对模式。当用户需要首次配对、清除旧连接后重新配对，或连接排障需要重置配对关系时，明确展示这组按键和后续在 macOS“系统设置 → 蓝牙”中重新连接的操作；不能把进入配对模式误写成 App 已自动完成蓝牙重置。
  - [ ] 在 Onboarding 中为豆包输入法提供专用配置指引：用户选择豆包后，提示进入豆包输入法的“语音输入”设置，将“长按模式”快捷键设为 `fn`、开启“全局唤起语音”，并把“麦克风选择”设为 `MiRemoteV 2ch`。向导应逐项展示这三项配置并在真实文字测试中验证最终链路；不要把豆包配置套用到 Typeless、闪电说等独立语音工具。
  - [ ] 在 Onboarding 新手引导中增加“打开 App 并聚焦输入框”的演示动画：清楚展示用户按下已映射的遥控器按键后，无线麦打开或切换到目标 App，并自动把焦点放入可输入文字的区域。动画用于解释这一核心能力，应支持重新播放，并与后续真实按键测试和自定义 App 配置衔接。
  - 每一步必须显示“用途、当前实时状态、一个主要操作、验证结果和失败后的下一步”。完成状态来自权限 API、蓝牙/HID、音频设备和真实语音数据检测，不能因为用户访问过页面或点击过系统设置就标记成功。
  - 流程不提供“跳过”或“稍后继续”，必要步骤未通过时不能进入下一页或主面板；关闭 App 后保存当前位置，下次启动直接恢复。设置页提供“重新运行设置向导”入口，并明确说明重新检查不会清除现有连接、按键映射或其他设置。
  - 从系统设置返回 App、App 重新获得焦点、重启、遥控器重连或音频设备变化时自动刷新当前步骤。对于输入监控或辅助功能需要重新勾选、关闭后开启或重启 App 的情况，提供准确操作和“重新启动无线麦”按钮，并在重启后恢复到原步骤。
  - 真实语音测试分别确认语音会话开始、实际 PCM 样本到达、所选语音输出设备就绪、松开后会话结束和文字实际出现在向导输入框；测试内容不由无线麦保存或上传。
  - 已完成 Onboarding 后权限被撤销或设备失效时，只在主面板显示对应修复提示并直接打开失败步骤，不强迫老用户重跑完整向导；流程使用版本化状态，新版本只补跑新增或失败的必要检查。
  - 重新布局“连接”页面，按“RC003 连接与语音状态 → 主要连接/重连操作 → 手机 App 与网页版等备用入口 → 对应排障信息”的顺序组织；当前状态和最常用操作优先显示，错误信息直接关联可执行的解决步骤，避免设备状态、语音触发和多种手机连接入口混在同一视觉层级。
  - 重新布局“按键映射”页面，按“遥控器实物图与当前选中按键 → 当前按键动作配置 → 映射启用状态与必要说明 → 高级或实验选项”组织；保留全部现有映射能力、麦克风键固定行为和实物图点击定位，不因布局调整改变按键含义、默认配置或持久化格式。
  - [x] 已按确认设计完成“连接”和“按键映射”两个现有设置页的重布局：连接页保留实体遥控器、语音输出、MiRemoteV 2ch、Nearby、TestFlight、受信任设备和网页版全部入口；按键页同时展示所有按键的单击、双击、长按，并默认锁定正在编辑的按键。
  - [x] 已实现 Typeless 式双栏 Onboarding：顶部仅显示“准备 › 设置 › 试一下”，不显示步骤编号、总数、百分比或强制提示；流程状态版本化持久保存，完成前启动时强制显示向导且不启动完整运行时，进入设置阶段后才启动蓝牙和音频服务。
  - [x] 自动化已覆盖步骤顺序、前后导航、启动窗口与延迟运行时门禁、所有能力门禁、退出续接、完成版本和重新运行；Onboarding 定向测试通过。完整 Swift 测试 178 项、18 个 suite 以及 42 项项目自检全部通过，Release 构建和测试 App 代码签名校验通过。无需屏幕解锁，已用生产视图离屏生成并逐页检查浅色、深色各 8 张真实实现截图，`1020 × 772` 页面无裁切且两种外观左右视觉体系一致；截图能力保留为 App 隐藏入口和本机 Skill。`v1.8.8 (100)` 已作为公开 Pre-release 发布，最终 ZIP 锁屏启动存活且无新增崩溃，公开下载资产逐字节校验和从已安装 `1.8.7 (68)` 发现候选更新均通过。
  - 2026-08-11 候选修复升级首次启动时“HID 按键已收到但 BLE 仍显示未连接”的停滞：向导只请求一次恢复，且空 bridge 的“重新查找”会实际启动蓝牙连接；仍待真实 Sparkle 升级和 RC003 基线验收。
  - 2026-08-11 已修复旧版已安装用户升级后被误送入完整 Onboarding：旧 Build 或 Sparkle 启动记录会在没有任何向导状态时一次性迁移为已完成；真正全新安装、中途未完成和主动重新运行仍必须继续向导。自动化已覆盖边界，等待真实 Sparkle 升级验收。
  - 2026-08-11 已修复音频步骤只接受 MiRemoteV 2ch：页面内平铺当前音频设备，BlackHole 2ch 和其他已安装设备也可选择；继续条件统一为“所选设备存在且生产音频输出已就绪”，最终仍由下一步真实文字输入阻止错误设备通过完整向导。
  - 2026-08-11 候选修复新配对遥控器需要重启才能显示、BLE 已连接但普通按键无响应：从系统蓝牙设置返回时刷新 discovery；多个 HID service 部分应用电源键保护时只监听安全 Location，失败或缺失 Location 继续关闭；设备卡改用“小米蓝牙遥控器 2 / 2 Pro”完整名称并自适应状态布局，连接页去除重复信息，文件菜单新增日志目录入口。208 项 Swift 测试、42 项项目自检、16 项硬件模拟、Release 构建和浅/深色生产 Onboarding 截图已通过，仍待 RC001 / RC003 新配对、双设备、电源键和电池充电状态真机验收。
  - 2026-08-11 已审计全部八页恢复门禁：遥控器页回到前台时同时刷新 BLE/HID，显示生产 HID 状态并提供重新检测；音频页回到前台自动刷新；完成页重新验证当前权限、BLE 和音频，同时避免依赖视图重建会丢失的临时语音/按键标志。对应自动化已加入，真实权限、热插拔、驱动和第三方语音工具仍待完整验收。
  - 2026-08-13 已加入本地首次成功漏斗与卡点自愈：记录进入、阻塞、重试、恢复、通过、完成、失败码和阶段耗时，相同轮询状态去重；每个阶段只突出一个主要修复动作，完成页按权限、遥控器或音频状态定向回跳。用户可复制脱敏设置诊断，内容不含设备名、设备标识、路径、用户文字或音频，也不会上传。自动化已覆盖失败码、事件去重、摘要和回跳，真实权限/硬件/第三方语音工具仍按测试手册验收。
  - 2026-08-15 遥控器页补充首次连接顺序：先长按 TV 键约 2 秒直到遥控器底部白灯闪烁，再同时长按 Home + Menu 进入蓝牙配对；现有连接和实体按键门禁不变。
  - 2026-08-13 权限依赖复核结论：普通按键映射仍需要输入监控与辅助功能；Typeless 的 Fn 点按至少需要辅助功能。当前没有豆包或其他工具在移除权限后的完整真实流程证据，因此候选继续保留现有三项首次门禁，不根据推测放宽。
  - 仍需在 macOS 14、15 和当前系统上验证全新安装、逐项拒绝后恢复、退出后续接、App 重启、权限被撤销、遥控器断连、音频设备缺失、豆包/Typeless/其他工具及完成后的首次真实使用；使用 `1020 × 772` 窗口逐步检查 Onboarding 和“连接、按键映射、权限”页面，确保页头、状态、主要操作、滚动和底部导航均不裁切，也不改变窗口几何。
  - 详细步骤、状态模型、错误分支、导航结构、实现切片和验收矩阵保存在私有产品资料库；公开 TODO 只保留产品行为和兼容边界。
- [x] ~~自动聚焦输入框~~（已完成：打开 Codex、Claude、cmux 后自动聚焦其输入区域；内置 App、自定义 App和自定义快捷键现统一等待 system-wide Accessibility 最终焦点成为安全可编辑位置，再允许第一次 Fn 语音启动）
- [ ] 支持通过遥控器按键一键唤起 Codex 并启动其语音功能（仅记录，暂不开发）
  - 第一层“打开或切换到 Codex，并聚焦输入框”已经具备技术基础；目标是将其与“启动 Codex 自带语音输入”组合为一个可配置动作，用户按一次遥控器按键即可开始说话。
  - 实现前先确认 Codex 是否提供稳定的全局快捷键、菜单命令、URL Scheme、App Intent 或其他公开入口。若存在，优先调用公开入口；若只有可访问性树中的麦克风按钮，可评估通过辅助功能执行 `AXPress`，但不得依赖固定屏幕坐标。
  - 组合动作需要按顺序处理“启动或激活 Codex → 等待窗口和输入区就绪 → 聚焦输入框 → 触发语音功能”，并覆盖 Codex 尚未启动、窗口被关闭、正在其他任务、首次麦克风授权及语音功能不可用等状态。
  - 需要确认再次按键是停止 Codex 语音输入，还是只负责首次唤起；在入口和停止语义未验证前，不承诺单键完整控制录音生命周期，也不能影响现有 Codex 自动聚焦及普通打开应用动作。
- [ ] 将输入设备与按键动作两端配置化，并评估用户共享动作市场（自定义 App 动作第一阶段已完成） <!-- workshop:status=进行中;priority=P1 -->
  - [x] 自定义 App 动作第一阶段已完成并经用户真机验收：Mac 端支持从本机选择任意 `.app`，将“只打开 App / 激活后发送聚焦快捷键 / 聚焦用户记录的 Accessibility 输入框”绑定到单击、双击或长按，并随多遥控器配置及导入导出保存。同一触发方式切换当前动作时，只改变启用项，不会清除已录入的自定义快捷键、已选自定义 App 或该 App 已学习的输入框；切回后可直接继续使用，恢复默认时才清除按键级配置。旧 `ButtonAction` raw value 和旧配置继续兼容。
  - 用户补充了一个更具体的第一阶段需求：例如 Proma 安装在 macOS“应用程序”目录后，只要配置其精确 Bundle ID，就应能作为自定义 App 被唤醒；用户再通过“记录目标输入框”选择要聚焦的输入区域，App 保存稳定的 Accessibility 特征，之后无需拆包、改源码或跟随无线麦频繁重新编译，即可把“打开 Proma → 聚焦输入框”映射到遥控器按键。
  - 动作端先配置化固定且通用的部分：显示名称、Bundle ID、启动或激活方式、聚焦策略、目标输入框的 role / identifier / placeholder / 祖先上下文等 AX 特征、重试和超时。输入框记录必须提供测试、重新记录和失效提示；目标 App 升级导致 AX 树变化时不能误聚焦搜索框、设置框、Token 或密码输入框。
  - 输入端也使用“物理设备 → 标准化控制项 → 单击 / 双击 / 长按 → action ID”的 Profile：对于标准 HID 键盘、媒体遥控器和游戏手柄，可以配置设备匹配条件、usage page / usage、按下松开和逻辑按键名称，使输入器不再局限于某个遥控器型号。私有 BLE 配对、ATVV 音频、特殊 report 解码或厂商协议仍需要受测代码适配，不能声称任何设备都能只靠配置支持。
  - 当前 `ButtonAction`、预置 App 和执行分支仍是封闭枚举；正在引入的设备 Profile 主要解决按设备保存映射，并不等于动作定义、App 聚焦规则或新 HID 控制项已经完全数据驱动。目标架构应统一为“输入设备 Profile → 标准化触发 → 稳定 action ID 与版本化参数 → 受控执行器 → 明确结果”，并允许 AI 按公开 Schema 生成可导入配置。
  - 第一阶段只做本地配置、记录、测试和导入导出，优先完成“自定义 App + 输入框聚焦”和标准 HID / 游戏手柄控制项；宏工作流、多个步骤之间的条件判断和共享市场后置。后续动作包可以再支持快捷键、受限 URL Scheme、用户已经创建并明确选择的 macOS 快捷指令，以及由多个安全动作组成的顺序工作流。
  - [ ] 按键映射新增“宏按键库”子页面：用户先在独立页面创建、命名、测试和保存可复用动作，再由遥控器的单击、双击或长按引用稳定的宏 ID，不再在每个具体按键下面临时重复配置目标 App、快捷键和聚焦方式。第一版宏至少覆盖自定义快捷键、打开或激活 App、发送聚焦快捷键，以及打开 App 后聚焦用户学习的 Accessibility 输入框；输入框学习、重新学习和测试入口必须直接显示在宏编辑页面。已有按键配置需要无损迁移并继续支持导入导出；同一宏可以被多个按键或设备引用，编辑和删除时必须明确展示影响范围。宏定义与物理按键绑定从一开始就解耦，为后续官方模板和用户共享市场保留版本、来源、能力声明和本地覆盖字段，但第一版仍只做本地动作库，不开放任意脚本或社区执行代码。
  - [x] 组合动作新增“运行快捷指令”步骤：自动读取并在 App 回到前台时刷新本机快捷指令，页面内搜索选择并显示名称、Identifier 与可用状态，支持单步测试；执行固定使用 Identifier，不通过 shell。Schema `0.2-draft` 支持导入导出名称和 Identifier，并明确声明“依赖本机快捷指令”，不导出快捷指令本体；目标 Mac 缺少对应 Identifier 时安全停止。软件自动化已覆盖，真实快捷指令、首次隐私授权、iCloud 同步、前后台和跨 Mac 导入仍按测试手册人工验收。
  - [x] 组合动作常用功能新增“打开网址”：在步骤卡片内填写完整网址，由系统默认浏览器打开；仅允许 `http` / `https`，拒绝本地文件、自定义 Scheme、账号密码、缺少 Host、控制字符和超长网址。支持保存、撤销和导入导出，软件自动化使用注入式打开器覆盖校验与执行边界；真实默认浏览器、前后台及遥控器触发仍按测试手册人工验收。
  - [x] 组合动作新增“执行已有组合动作”：在步骤卡片内搜索并选择本机已有动作，运行时按稳定 ID 解析最新已保存版本；当前动作不出现在自身候选列表。保存前检测直接和间接循环，运行时再次防御损坏配置，最多嵌套 8 层；导入导出只记录名称和稳定 ID，不复制被引用动作。软件自动化已覆盖最新版本、缺失依赖、循环与深度边界，真实遥控器链路仍按测试手册人工验收。
  - 当前状态：私有 `sayall-macro-platform` 已提供可选无线麦集成模块，公开 App 仅保留薄适配层；功能由独立 Feature Flag 与邀请码资格控制，未包含私有模块或资格无效时不显示入口、不接管按键。2026-08-14 已修复生产资格时间解析导致的客户端无反馈，并同步修复共享资格客户端；邀请码区域现使用最终 App 内私有 Bundle 的中英文资源，紧凑合并输入、验证、状态和隐私说明，宿主 Edit 菜单恢复标准文本编辑快捷键。同日确认 `1.8.22 (114)` 的快捷指令实际页面仍有三处直接 SwiftPM 资源访问，点击会崩溃；现已统一到标准 App 资源解析器，并在私有模块和宿主构建脚本增加禁止页面绕过解析器的门禁。2026-08-16 继续统一组合动作编辑页的信息层级：固定遥控器预览尺寸、突出测试与保存、名称限制为 20 个字符、对齐步骤标题字号、显示目标 App 图标和 Bundle ID，并扩充常用预置 App；宿主侧边栏改用显式顺序表，主页面相对顺序为按键、组合动作、统计、连接、权限、关于。2026-08-17 遥控器绑定区新增保存前后动作提示：绑定按键页已有动作或替换其他组合动作时，直接显示当前会执行什么和保存后会执行什么，不新增弹窗。等待新的 Developer ID 安装包、有效资格真实点击、重启恢复、真实键盘和真实遥控器人工验收后再标记完成。
  - [ ] 支持按遥控器型号保存、预览、一键应用和上传完整布局模板：模板把标准化物理按键及单击、双击、长按绑定到宏 ID，可以为“小米遥控器 2 Pro + Codex”“小米遥控器 2 Pro + OpenCode”等常见组合提供开箱即用布局。应用前必须展示将覆盖的键位、所需 App、宏和权限，缺失依赖或需要学习输入框时先完成本机配置，再原子替换当前布局；发布时移除设备标识、私人输入框特征和其他本机数据。实施顺序为本地模板导出导入、官方精选模板、最后开放社区上传、搜索、评分、版本更新和回退，用户修改已安装模板后不得被市场更新静默覆盖。
  - [ ] 建立参考 TourBox 的预设中心：把随 App 提供且离线可用的“系统预设”、无线麦审核维护的“官方精选”、用户上传的“社区预设”和尚未发布的“我的预设”明确分区并显示来源标识。支持按遥控器型号、目标 App、使用场景、语言和作者筛选或搜索，优先为小米遥控器 2 Pro 提供 Codex、OpenCode 等系统布局；社区预设必须经过安全校验并展示作者、版本、依赖、兼容设备、安装量或评分等信息，不能与官方预设混淆，也不能因下架或市场不可用影响已安装的本地副本。
  - 不允许市场动作默认执行任意 Shell、AppleScript、动态库、下载后的可执行文件或未声明的网络请求，也不能直接绕过现有白名单让手机或网页下发任意命令。高风险能力即使以后开放，也必须逐项授权、显示将要执行的内容，并提供撤销、禁用、审计和失败回滚。
  - “开放接口”和“建设市场”分阶段实施：先验证本地动作包、文档、示例和配置迁移，再考虑官方精选目录，最后才评估社区发布、搜索、评分、举报、签名、审核、依赖、版本兼容、下架、更新回滚和恶意内容治理；市场不可阻塞当前独立语音输入 V1。
  - 该能力可以覆盖 Codex 之外的自定义 Agent，但不能因此删除现有稳定内置动作；旧配置、iOS 和网页版仍使用稳定动作标识，无法识别扩展动作时必须明确显示不可用，而不是执行其他默认动作。详细可行性、接口草案、安全模型和实施顺序保存在私有产品资料库。
- [x] ~~配置导入导出~~（已完成：支持版本化 JSON 导入导出全部个性化设置；本地使用统计不随配置迁移）
- [x] 兼容 Typeless 等点按切换式语音输入
  - 默认关闭的“语音键模拟 Fn 点按”模式把按下和松开转换为两次 Fn 点按，同时保留 RC003 必须物理按住才能采音的硬件行为；这不是持续录音或独立语音输入。
  - 功能入口位于“按键映射”页遥控器下方，与语音键的固定按住说话行为放在一起说明；连接页不再重复展示该设置。
  - 已完成辅助功能权限门、全目标 HID neutralize 与失败回滚、generation 会话状态、开头 pre-roll、结尾排空、配置兼容及自动化测试；新增跨组件首次语音门禁，覆盖目标延迟 0～3 秒、5 秒缓存、提前松开、超时、目标切换、敏感字段以及 RC001/RC003 模拟 `STREAM_START → AUDIO → STREAM_STOP`，不再把第三次成功视为通过。待实体遥控器复验默认豆包路径、Typeless 路径、录音中关闭开关、断连恢复和唤起目标后的第一次文字上屏，再标记完成。
  - `1.7.6` 预览版已因 macOS 26 启动阶段的 HID 服务生命周期崩溃撤回；修复后服务对象会在映射读取和写入期间持续持有所属 HID 客户端，并由生命周期回归测试覆盖。`1.7.7` 已在 RC003 连接和代表性持久化设置下通过最终 ZIP App 的首次启动、正常退出、二次启动、四种功能状态及无新增崩溃报告门禁；PKG 内嵌 App 与已启动验证的 App 完全一致，已发布为 Pre-release。Typeless 等实验路径仍按本条后续真机范围继续验证。
- [ ] 语音键触发键可配置（Fn / 右 Command / 右 Option / 右 Shift）
  - 语音键默认仍硬件映射为 Fn，兼容豆包输入法与系统听写；新增在“按键映射”页语音键卡片下方选择触发键，可改为右侧 Command、Option 或 Shift，方便第三方语音软件用右侧修饰键作为“按住说话”启动键。仅改变发出的“触发键”，不改按住说话语义、音频串流与 hold/tap 状态机。
  - 新增“用遥控器自带麦克风收音”开关（默认开）：关闭后语音键变纯触发器——按住只发送触发键（IOKit 硬件重映射），不采集/不路由遥控器 ATVV 音频，可配合外接麦克风（如大疆）作为语音输入源，实现“遥控器触发 + 外接麦收音”；开启则为现状（触发键 + 遥控器麦克风进虚拟麦）。关闭时 Typeless(Fn 点按) 自动禁用（其注入依赖遥控器音频排空）。默认开、旧配置缺字段回退为开，行为与现状逐字节一致。
  - 触发键设置持久化并纳入配置导入导出（缺字段回退 Fn）；默认 Fn 与旧版行为逐字节一致。硬件重映射（按住模式）与 Fn 点按注入（Typeless / 手机语音）两条路径统一使用所选触发键。
  - 已完成单元测试：触发键常量（HID usage / keyCode / flags）、mapper 按触发键重映射、默认 Fn 回归、注入键码与修饰、配置导入导出往返；本地化中英文键对齐。待真实 RC003 + 第三方语音软件端到端验收：右 Cmd/Option/Shift 按住说话被目标软件识别、松开结束、音频进入虚拟麦，以及默认 Fn 豆包路径回归。测试手册见 [`Testing/VoiceTriggerKey.md`](Testing/VoiceTriggerKey.md)，当前未完成真机验收。
- [ ] 普通遥控器语音键突破一分钟录音限制 <!-- workshop:status=阻塞;priority=P2 -->
  - 曾尝试在普通物理语音会话收到 `STREAM_START` 后每 10 秒调用 ATVV v1.0 `MIC_EXTEND`，并设置 180 秒关闭与 2 秒超时重连。
  - 2026-08-11 真机日志确认普通物理会话没有 `microphoneOpened` 状态，定时调用持续返回 `ATVV MIC_EXTEND rejected`，命令并未写入遥控器；模拟租期模型不能证明真实固件行为。
  - 上述候选方案已经撤回：普通会话不再定时续期、不再由 Mac 强制限制为三分钟，也不再因关闭确认超时重连。底层命令只保留给未来成功主动 `MIC_OPEN` 的独立实验。
  - 当前问题仍未解决，需要重新取得 RC003 约 60 秒停止的完整真机日志，再基于真实控制/音频事件顺序研究固件边界。
  - RC003 长录音、稳定语音基线和 iOS 点按/按住录音的真机步骤统一维护在 [`Testing/ExtendedVoiceRecording.md`](Testing/ExtendedVoiceRecording.md)。
- [ ] 开关键支持开关式长时间录音 <!-- workshop:status=阻塞;priority=P2 -->
  - 用户主动开启持续录音实验功能后，将遥控器开关键临时绑定为长时间录音开关：第一次点击开始录音，录音期间不需要持续按住；再次点击立即结束录音。关闭实验功能后恢复原按键映射；语音键继续保持现有的“按住说话、松开停止”逻辑。
  - 默认最长录音时间为 1 分钟，并允许在设置中选择 1 分钟、3 分钟、5 分钟或无限时长。达到所选上限后自动停止；用户再次点击可以随时提前结束。
  - 录音期间必须提供明确的持续录音状态和剩余时间提示；无限时长模式下，遥控器断连、App 退出、音频链路失败或用户主动停止时仍必须可靠关闭麦克风，不能留下后台持续收音状态。
  - 已确认 RC003 的语音键正常硬件路径是“按住收音、松开停止”，单击本身不会保持收音。需要先验证实体开关键是否会通过蓝牙发送 macOS 可识别的 HID 事件；触发后由 App 主动发送 `MIC_OPEN`，并验证 1/3/5 分钟和无限模式能否通过 ATVV v1.0 的 `MIC_EXTEND` 稳定维持。
  - 已进入核心真机验证阶段：持续录音作为默认关闭的本地实验功能，仅在用户主动开启后自动绑定开关键，并保留关闭前的原映射；核心验证包含主动 `MIC_OPEN`、每 10 秒 `MIC_EXTEND`、再次点击关闭及固定 1 分钟安全上限。待真实 RC003 验证通过后，再开发 1/3/5 分钟与无限时长设置、完整倒计时和其余产品化界面。
  - `1.6.10` 预览验证发现持续录音的会话门禁误拦截普通语音键直接发送的 `STREAM_START`，同时新增说明使按键映射页超过窗口高度；已按普通语音兼容边界和可滚动页面约束修复，持续录音真机状态仍保持“验证中”。
  - 最新 RC003 真机对照确认：Power 主动 `MIC_OPEN` 会返回 `STREAM START session=0`，但整个会话没有任何 audio characteristic 数据；同一连接随后使用普通语音键会立即收到连续 120-byte 音频包。控制状态不能视为真实录音成功，当前固件上的实验入口已撤下，旧设置会自动关闭并恢复 Power 原映射；后续只有找到新的固件级可行路径后才重新开放验证。
- [x] ~~修复语音结束后立即按电源键时偶发触发系统锁屏或关机确认框~~（已完成）
  - 用户复现：把遥控器电源键映射为回车，语音说完后立刻按下，容易泄漏原始电源键系统行为并出现锁屏或关机确认框，而不是只执行回车；等待约 0.5–1 秒再按则正常，且语音持续越久似乎越容易出现。
  - 持续录音真机验证确认无需先结束语音也可稳定复现：App 在同一秒成功执行 `toggleLongRecording`，`loginwindow` 同时收到原始 Power 事件并进入 Software Sleep。RC003 无法被普通用户进程独占，现有 Session Event Tap 对系统电源路径不可靠；根因不是持续录音动作本身，也不能通过延长 180ms 窗口可靠解决。
  - 已改为在启用自定义映射时，使用 RC003 专属 HID `UserKeyMapping` 将 Keyboard Power (`0x66`) 预先映射为无危险的 F20 (`0x6F`)，再启动 HID 监听；映射失败时按键监控失败关闭，退出或关闭自定义映射时恢复原 Power 映射。单元测试和项目自检已覆盖应用、恢复及失败关闭；RC003 真机复验确认自定义动作正常执行、电脑不再锁屏，且系统日志不再出现对应 Power 事件。
  - 相关 UX 观察：方向键当前在按住 350ms 后进入连续触发，方向键每 100ms 重复一次；边界时序下可能出现用户认为“点一下却走两格”的感觉。这是现有设计，暂不单独判定为 Bug；若反馈增多，再评估延长首次连发延迟、增加设置项或优化按下/松开判定。
  - 2026-08-12 修复非独占 HID 监听下实体方向键被长期拦截：App 定时连发不再重复登记物理 `.down`，monitor 在断连、停止或权限撤销时会释放仍按住的 suppressor 状态。硬件模拟已覆盖全部 11 个原生按键的正常松开、按住期间断连和双遥控器共享计数，并验证返回键不进入抑制器；等待真实 RC001/RC003 与 MacBook 键盘复验。
- [ ] 优化语音降噪与人声放大
  - 在遥控器和手机麦克风音频进入虚拟麦克风前增加可控的降噪、人声增益、自动增益和限幅处理，优先提升远距离、小音量说话时的可懂度。
  - 不能只提高现有固定增益，否则会同时放大底噪并造成削波；需要分别评估高通滤波、噪声抑制、AGC 和 limiter 的组合，并提供关闭或强度调节能力。
  - 使用 RC003、手机麦克风和后续兼容遥控器分别验收安静、风扇声、键盘声及较远说话场景；要求人声更清晰且无明显抽吸声、失真、截断、延迟或 CPU 占用异常。
- [ ] 根据用户选择的首选语音输入工具，在按下麦克风键时自动准备对应输入环境（仅记录，暂不开发） <!-- workshop:status=待处理;priority=P1 -->
  - 设置中允许用户选择“不自动处理、豆包输入法、Typeless、闪电说”等首选语音输入工具；功能默认关闭，只有用户明确选择豆包输入法并开启自动切换后，才在麦克风键按下时尝试切换。
  - 初步结论是豆包路径技术上可行：macOS 提供公开的输入源枚举、当前输入源读取和输入源选择 API，可切换到已经启用且允许选择的输入法。实现前必须取得并保存用户机器上豆包的真实 Input Source ID，切换后验证当前输入源确实变化，再触发现有语音键流程；不能按显示名称模糊匹配，也不能在豆包未安装、未启用或切换失败时假装成功。
  - Typeless 和闪电说属于独立语音输入 App，不是 macOS 输入法，不能使用输入源 API 切换。第一版选择它们时不执行输入法切换；是否可以通过各自公开接口、快捷键或 URL Scheme 联动需要另行验证，未经验证不得自动启动或操作第三方 App。
  - 需要记录切换前的输入源，但录音结束后不能立即假定可以恢复，否则可能打断豆包识别或上屏。必须真机验证识别完成时机，再决定保持豆包、延迟恢复或提供用户选项；任何恢复都只能在用户没有手动切换输入源时执行。
  - 自动切换是辅助功能，失败时应提示并继续现有录音链路，不得影响遥控器语音、虚拟麦克风和普通按键行为。
- [x] 新增 pre-release 发布，避免未实测的功能被检测到更新
  - “关于”页提供默认关闭的预发布更新开关；仅在用户主动开启后，Sparkle 自动与手动检查才会包含最新候选版本。
  - v1.7.3 修复预发布源解析失败或缓存旧地址时阻断正式版检查的问题；预发布源不可用时必须清除旧地址并回退到稳定更新源。
  - 候选版本不存在、GitHub API 限流、网络超时或候选源暂不可用时静默忽略，不显示错误弹窗；手动检查继续使用稳定源。
  - macOS 候选统一使用从最新 `main` 创建的 `release/pre-vX.Y.Z` 分支；功能必须先通过 PR 合入 `main`，发布会话不再临时整合开发 Commit。候选分支只保留一个版本、ReleaseHistory 和测试说明提交，禁止从旧预览分支或 Tag 串联下一候选。Push 后由 GitHub Actions 自动完成来源校验、三条 workflow 私有依赖钉定一致性、完整 Mac 测试和临时 App 打包。
  - 精确候选 SHA 的 Apple Silicon、Intel Job 成功后，发布会话提前创建 Draft 回流 PR，使受保护 PR CI 与正式双架构签名、公证并行；正式打包复用该精确 SHA 的候选测试证明，并通过独立 SwiftPM scratch 隔离两种架构。Pre-release 资产完成发布、公开下载字节和 provenance 校验后，Release Guard 才将 Draft PR 转 Ready 并启用自动合并；这只把候选提交记录回 `main`，不会把 GitHub Release 晋升为正式版。下一预览版必须从回流后的最新 `main` 创建。
  - 正式版只能选择已经发布并验证过的 Pre-release，复用完全相同的 Tag、签名、公证资产和摘要；候选回流 `main` 不构成正式发布授权，禁止从 `main` 重建正式资产。
  - 2026-08-17：正式晋升工作流补充独立 `mac-stable-release` Environment 和按需安装 `ripgrep` 的工具门禁；晋升仍只复验并提升既有候选字节，不读取 Apple 签名 Secrets，也不重新打包。
- [x] 在“关于”页集中展示版本与更新信息
  - 将当前版本、可更新版本、检查更新、版本历史和预发布更新开关集中在同一模块；语言选项保持全部平铺，移除术语表入口。
  - 检测到新版本后显示当前界面语言对应的用户可见更新内容；缺失本地化说明时仍可继续使用 Sparkle 更新，不阻断检查或安装。
  - 自动化、候选资产和跨版本升级验证全部通过后，再将本条标记完成并发布 `1.8.7` 预发布版本；真实用户环境中的可见界面验收继续按测试手册记录边界。
  - 2026-08-23：版本说明改为带 `⚠️/🎉/✨/🐛` 分节后，「版本历史」Sheet 原先把每个 `## ` 都当成一个版本，分节标题会变成空卡片。现只在版本号开头的标题处新开卡片，分节标题并入当前版本；真实窗口渲染验收见 [`Testing/AboutUpdateCenter.md`](Testing/AboutUpdateCenter.md)，尚未完成。
  - 2026-08-23（复核后续）：上一条留下两个缺陷，已修复。其一，`compose-release-body.sh` 把两种语言各自包在 `## 更新内容` / `## What's New` 下，这两个标题与规范要求的四个分节同级，用户看到的第一个标题不是必须置顶的 `## ⚠️ 注意事项`；现在两半之间只用分隔线，正文第一个标题就是该语言的分节标题。其二，分节允许清单只看开头的表情符号，`## 🎉 9.9.8 …` 这类以分节表情开头的版本标题会被当成条目内部结构，把下一个版本的条目并进来。第一版改法（标题含数字即视为版本标题）在复核中被否决：它会让 `## 🐛 问题修复（第 2 批）` 在条目中途终止，整节修复连同列表项一起从 GitHub 正文和 Sparkle 说明里消失，而两个调用方的 `- ` 门禁只在一条列表项都不剩时才触发，因此全链路退出 0、无任何报错。现在的规则是：与八个规范分节标题逐字节相等才算条目内部结构；只是以分节表情开头但不完全相同的标题按歧义处理，非 0 退出并指出标题和文件，让人改标题；其余标题照旧终止条目。GitHub 正文分节与语言分界的验收项见 [`Testing/MacReleaseBranchLifecycle.md`](Testing/MacReleaseBranchLifecycle.md)，复现、传播与负向对照见 [`Bugs/2026-08-23-release-notes-swallow-every-matching-version.md`](Bugs/2026-08-23-release-notes-swallow-every-matching-version.md)。
  - 2026-08-23（第三轮复核）：又发现三处，均已修复。其一，`Resources/Info.plist` 仍是上游 `1.8.25`，而版本历史最新条目是 `1.8.25-fork.4`，发布会打出 `v1.8.25` 且没有任何一处比较过两者；现在 `publish-release.sh` 在制品校验与网络调用之前要求被发布的版本必须是两份版本历史各自的最新条目，`VERSION` 与来自环境变量的 `${RELEASE_TAG#v}` 都要过门，拒绝时指出找的版本、来源、文件和实际最新条目，`notarize-release.sh` 在构建前补了同一道门（它把说明签进 Sparkle 与 appcast，原先只有 `- ` 门禁）。其二，标题原为 `Remote Mic $VERSION`，既无关键词也正是规范点名反对的写法；现在为 `v<版本>: <关键词>`，关键词由环境变量 `RELEASE_TITLE_KEYWORDS` 传入（与 `RELEASE_TAG`、`DRY_RUN` 一致，且 `promote` 不写标题），缺失、空白、多行或自带版本前缀一律非 0 退出，绝不退化成通用标题，正文另加「不得含 H1」门禁。其三，版本查找是前缀匹配，`1.8.25`、`1.8.2`、`1.8.1`、`1.6.1` 四个真实版本各自返回了别人的说明且退出 0，上游 `## 1.8.25（预发布）` 取不到；现在逐字节精确匹配，只允许两份文件实际使用的括号标签，并新增 `--newest-version` 让「标题代表哪个版本」只有一份实现。**版本号与 Tag 格式已由用户确定并落地**：保留 `-fork.N` 后缀，`Resources/Info.plist` 改为 `1.8.25-fork.4`，Build 从 122 递增到 123（122 已随 `v1.8.25-fork.3` 发布，Sparkle 用 `CFBundleVersion` 判断是否更新，沿用会让 fork.3 用户收不到 fork.4；`verify-preview-branch.sh` 也要求 Build 必须大于上一个 Tag 的 Build）。`publish-release.sh` 的 Tag 规则由 `^v[0-9]+\.[0-9]+\.[0-9]+$` 放宽为只多接受一个 `-fork.<数字>` 序号，空后缀、非数字序号、多个后缀、`-rc.1` 这类其他后缀和任何多余文字仍然拒绝；`verify-preview-branch.sh`、`verify-preview-candidate-ci.sh`、`reconcile-release-event.sh`、`fast-release.sh` 和 `.github/workflows/mac-stable-promote.yml` 五处同形状规则一并放宽，否则真实发布会在候选分支校验、Release Guard 或正式晋升处被自己的门禁挡住。**仍未完成**：本机没有 Developer ID 证书，签名、公证、appcast 生成和真实 Sparkle 安装更新都未执行，`DRY_RUN` 只能走到缺少已签名 Sparkle zip 处；「关于」页当前版本号由 6 字符变为 13 字符且以 28pt 显示，真实窗口未验收。验收项见 [`Testing/MacReleaseBranchLifecycle.md`](Testing/MacReleaseBranchLifecycle.md)，复现、调用方行为与负向对照见 [`Bugs/2026-08-23-release-notes-swallow-every-matching-version.md`](Bugs/2026-08-23-release-notes-swallow-every-matching-version.md)。
- [x] 改进更新完成和 App 启动时的主面板可见性
  - 更新安装成功并重启 App 后，将更新完成提示及 App 窗口置于最前方，避免弹窗被其他窗口遮挡而遭到忽略。
  - 更新成功并重启后自动打开主面板（设置面板）；此行为始终生效，不受普通启动开关影响。
  - App 普通启动时默认自动打开主面板，而不是只在菜单栏后台运行。
  - 在“关于”页面增加“启动时自动打开主面板”开关，默认开启；关闭后仅取消普通启动时自动打开，用户仍可通过菜单栏手动打开主面板。
  - v1.5.1 已完成：78 项 Swift Testing、36 项 Self Test、Release App 构建与结构验证通过；真实 `.app` 已验证默认启动显示、关闭开关后仅菜单栏常驻，以及模拟更新时无条件前台显示主面板和更新完成提示。
- [ ] 免按键持续语音控制，通过说话直接执行“打开 Codex”等操作
  - 当前结论：功能可行，优先使用 macOS 15 自带的“语音控制”完成零开发验证。系统支持持续听取、选择麦克风、离线使用、自定义命令和运行快捷指令；若“打开 Codex”不能直接识别，可创建打开 Codex 的快捷指令并绑定自定义语音命令。
  - RC003 的正常产品行为仍是按住语音键收音，但 Infineon 官方 ATVV 参考固件证明主机可以主动发送 `MIC_OPEN`，协议 v1.0 还定义了 `MIC_EXTEND`。RC003 是否接受无按键主动开麦、最长能维持多久、能否靠 `MIC_EXTEND` 续期，目前尚未真机验证，不能再写成已确认绝对不支持。
  - 产品路线仍优先使用 Mac 内置麦克风或带实体静音键和工作指示灯的 USB 麦克风；RC003 只保留为实验性音源候选，避免在续航、休眠和隐私状态尚未确认前承诺持续监听。
  - 第二阶段再评估无线麦内置模式：本地低功耗唤醒词持续监听，唤醒后仅短时识别白名单命令，并复用现有 `ButtonAction` / `KeyboardInjector` 执行动作。不能只依赖 `SFSpeechRecognizer` 无限持续识别，因为 Apple 官方说明单次音频约一分钟、服务可能限流且会增加电量和网络负担。
  - 产品模式必须默认关闭，提供明确的正在监听指示、快速暂停和麦克风选择；不保存或记录原始语音，危险操作需要二次确认，避免环境声音误触发。
  - 可行性、零开发验证步骤、实现分层、RC003 主动开麦协议证据和真机实验方案已迁移至独立的产品资料工作区，不随公开源码仓库发布。
- [ ] 解决组合快捷键配置问题，例如配置 Command + Space 唤起 Spotlight
  - [x] 录入器已从窗口内 `NSEvent` 监听改为仅在用户点击“录入快捷键”后短暂启用的 `CGEventTap`，可以在系统或其他 APP 的热键处理前捕获并阻止本次按键继续传播；保存格式和后续快捷键执行路径不变。
  - [x] 空配置不再显示“等待按键”，录入入口只在点击后出现；完成后显示明确成功反馈，权限缺失或事件 tap 无法启动时显示可操作的失败提示。
  - [x] 自动化已覆盖 Command + Space 事件捕获与阻止传播、只完成一次、忽略自动重复和合成事件、权限失败，以及页面结构和中英文文案。
  - [ ] 仍需在已启用 Spotlight 快捷键的真实桌面会话中确认 Spotlight 不弹出、页面显示“快捷键录入成功”，并用真实遥控器验证保存后的动作能够唤起 Spotlight；完成该人工验收后再勾选本条。
- [ ] 支持将单独的 Left/Right Option 等修饰键录入为快捷键
  - 当前快捷键录入器只监听普通 `keyDown`，而单独按下 Option、Command、Control 或 Shift 产生的是 `flagsChanged`，因此组合键可以录入，单独修饰键无法录入。
  - [x] 组合键的左右侧已保真：录制保留设备相关位，注入时按下真实侧别修饰键（右⌘=54、右⌥=61、右⇧=60、右⌃=62；左侧 55/58/56/59）并逆序释放，界面显示“左/右”。旧配置（无侧别）继续走原 flags-only 路径。详见 [`Bugs/2026-08-13-custom-shortcut-loses-modifier-side.md`](Bugs/2026-08-13-custom-shortcut-loses-modifier-side.md) 与 [`Testing/CustomShortcutModifierSide.md`](Testing/CustomShortcutModifierSide.md)；待真机验收。
  - 实现时需要等待修饰键释放以区分“单独修饰键”和“修饰键 + 普通键”，保留左右侧键码，并在合成按键时可靠清除释放事件的修饰标志，避免修饰键卡住；需覆盖单独 Left/Right Option 及现有组合快捷键回归。
- [ ] 支持通过遥控器完整操作 macOS `Command + Tab` 应用切换器（仅记录，暂不开发）
  - 用户触发配置的应用切换动作后显示系统 `Command + Tab` 切换器；切换器显示期间，遥控器左右方向键只移动当前 App 选项，按“确定 / OK”确认并切换到所选 App。
  - 需要将一次普通快捷键扩展为有生命周期的临时模式：合成并保持 `Command` 按下，发送 `Tab` 打开切换器，方向键继续选择，确认时释放 `Command`；不能在初次 `Command + Tab` 后立即释放修饰键，否则系统会直接完成切换。
  - 临时模式下需要拦截方向键和确定键，避免同时执行用户原有按键映射；按返回键、超时、遥控器断连、App 失焦或执行失败时必须取消模式并可靠释放 `Command`，防止系统出现修饰键卡住。
  - 需要真机验证左右方向键、循环选择、只有一个窗口、App 尚未启动、全屏空间、多显示器及系统动画关闭等情况；不使用自制 App 列表替代系统切换器。
- [ ] 支持苹果遥控器
  - 已完成 SiriRemoteForge、Wand、siri-remote-steamos、SiriRemoteVibe 及其他相关开源项目的可行性研究；当前总体首选 SiriRemoteForge，建议按“按键 → 触摸/滚动 → 麦克风高级组件”分阶段验证。
  - SiriRemoteForge 集成评估、候选项目源码与 Release 对比，以及暂停的 Apple Remote Windows 路线研究均已迁移至独立的产品资料工作区。
- [x] 支持 Xiaomi Bluetooth Remote Control 2（RC001-MS）
  - 已通过真机确认 RC001-MS 可以连接 Mac，除语音键之外的普通按键能够被现有 App 或 macOS 识别；语音键没有普通按键事件不代表设备没有语音能力，当前未知项是 Voice GATT / HID Report、音频包格式和编码方式。
  - 实现前先记录现有 RC003-MS 的服务、Characteristic、控制包、音频包和解码基线，再为 RC001-MS 增加只用于诊断的完整 Service Discovery、Notify / Indicate 订阅和受控数据日志；不得根据同系列型号猜测 UUID 或直接复制 RC003 常量。
  - 根据真机协议差异选择最小改造：优先只增加设备识别或 `RemoteProfile`，其次共享解码器但区分 UUID，只有传输或编码确实不同才增加独立适配层；RC001-MS 与 RC003-MS 的普通按键、语音、音频和断连恢复都通过回归后才能声明兼容。
  - 2026-08-08 双设备真机对照确认：RC001-MS 与 RC003-MS 在 macOS 中报告相同名称、固件 `2671`、VID/PID `0x2717 / 0x32B8`，暴露相同 GATT Service，并协商为 ATVV v1.0、16kHz IMA-ADPCM、120-byte frame；RC001 已完成真实 `STREAM_START → AUDIO → STREAM_STOP` 语音输入。正式实现只需泛化设备识别并复用现有协议与解码器；待正式构建完成重连、普通按键及两款设备回归后勾选完成。
  - 多遥控器开发版已支持按物理设备保存独立配置，并在连接页和按键页使用可直接点击的常驻设备卡片切换；卡片显示名称、连接状态、标准蓝牙电量和当前选择。普通按键不再要求用户手动绑定：首次识别到新的 HID 物理指纹时自动分配独立配置并立即执行按键，新接入遥控器先复制当前遥控器的完整快捷键，之后可分别修改。已用 RC001 + RC003 真机同时读取到 `42%` 与 `100%`，并完成两款设备普通语音、交替语音及 Fn 点按路线回归；仍需完成两只设备不同快捷键、自动切换和断连重连验收，因此本条保持未完成。
  - 正式设备信息读取已加入：连接后通过标准 `2A24` 自动识别 `RC001 / RC003`，界面直接显示“小米遥控器 2 / 小米遥控器 2 Pro”，不再提供型号和名称输入；RC003 同时读取可选 `2BED` 并在设备卡显示电池供电、外部电源或正在充电，RC001 回退显示电池供电。设置窗口默认和最小尺寸调整为约 `1020 × 772`，五个页面的顶部标题及按键/统计页顶部操作保持固定，只有正文滚动；按键状态副标题移入遥控器预览卡。每个 HID 监听器只处理对应物理遥控器的报告，避免同一次按键同时驱动两个配置；未绑定设备的 discovery 监听器改为由第一份真实按键报告决定物理遥控器，不再因系统先枚举另一只设备而让当前遥控器的单击和双击同时失效。自定义组合快捷键同时抑制 App 定时连发、硬件快速重复报告及按住期间泄漏给 macOS 的原始自动重复事件，避免 `Command + Return` 连续注入；用户录入的快捷键切换到其他动作或禁用后仍保留，返回自定义快捷键即可继续使用。基础按键补充 Command-Return、Shift-Return、Command-C、Command-V、Command-Q，系统与媒体补充 Command-← 与 Command-→，这些固定组合动作均禁止按住重复；“禁用按键”已从动作列表移到编辑标题栏的大号 Switch。方向和删除动作在无线麦自身位于前台时也会同时停止 App 定时连发与硬件重复执行，避免设置页持续播放系统错误音，同时保留其他 App 中的长按导航和系统音量连发。按键映射页已实现“遥控器居中、设置贴近对应按键”的单屏布局，12 个可配置按键和固定语音键均使用真实名称、当前设置及校准后的连线端点；“启用自定义按键”使用标题右侧的原生 Switch，缺少输入监控或辅助功能权限时会提示并进入权限页，授权返回后自动应用；“锁定当前按键”和“语音键模拟 Fn 点按”均显示简短用途说明。连线已改为从真实热点到卡片边缘的独立曲线，卡片向中间加宽以缩短连线并缓解两侧拥挤，菜单键和主页键固定连接左列，TV 键和音量减固定连接右列，两列均按实体按键高度顺序排列以避免交叉；连线图层从遥控器真实按键热点开始，静止时隐藏热点标识、实体按下时显示标识，箭头与卡片边框保留间距避免叠压。`1.8.3` 另外修复 Sparkle 升级后的首次运行没有恢复已开启 HID 监听的问题，升级完成后会自动延迟重建一次 monitors，无需用户手动关闭再开启开关。仍需插拔 RC003 充电线验证状态变化和通知能力，并完成上述双遥控器真机回归后再勾选本条。
  - 2026-08-10 修复 monitored fallback 下左右键按住不能连续移动其他 APP 文本光标：当物理左右键与方向动作完全匹配且没有辅助动作时改用 macOS 原生事件；非匹配映射仍保留 App 连发，并修正 timer 重复累加原生按住状态的问题。seized/monitored、无线麦前台和非匹配动作已通过硬件模拟，实体遥控器仍待 Preview 验收。
  - 2026-08-10 基础按键继续补充 Command-W、Command-X、Command-A、Command-Z、Command-Shift-Z、Command-F 和 Command-S；这些固定组合与既有 Command-Q 一样禁止按住重复。无线麦自身补上标准 macOS 菜单，可使用 Command-Q 退出、Command-W 关闭当前窗口。
  - 2026-08-13 基础按键补充 Command-Delete，沿用固定组合键的一次触发策略；动作分类、键码与 Command 修饰键已有自动化覆盖，真实遥控器和目标 App 行为仍需人工验收。
  - 2026-08-09 发现 1.8.0 中 RC001 短语音在 `STREAM_STOP` 时会被播放器立即清空；已用独立硬件模拟项目先复现失败，再改为保留待播放尾帧，并同时通过 RC001 短流与 RC003 `STREAM_START → AUDIO → STREAM_STOP` 自动化基线。随后完成 30 次真机会话：RC001 20 次、RC003 10 次，覆盖短语音、连续输入、双设备交替、普通虚拟音频和 Fn 点按路线；全部无入队失败、播放中断或停止残留，用户确认实际体验正常。该语音 Bug 已关闭，RC001 整体任务仍因其他独立回归项保持未完成。
  - 完整兼容性结论、代码改造建议、抓包实验步骤和记录模板保存在私有产品资料库。
- [ ] 支持更容易购买的低价 ATVV 语音遥控器 <!-- workshop:status=进行中;priority=P2 -->
  - 2026-08-08 已实际测试三款硬件：小米蓝牙语音遥控器第三方兼容版、Ugoos UR02 和音仕达录音笔。三款设备在当前真实 Mac 环境中均无法建立蓝牙连接，因此没有进入 GATT、ATVV、HID、音频格式或 App 适配阶段，当前均不能列为兼容或推荐设备。
  - 公开的 Linux / BlueZ 日志可以证明特定 UR02 样机曾暴露 ATVV v1.0 和 16kHz ADPCM，但不能证明同型号、同批次或当前样机能与 macOS 配对。此前将 UR02 列为优先工程样机的判断已经撤回，除非找到明确的 macOS 配对方法、固件差异证据或另一只样机完成复验，否则暂停继续投入。
  - “商品宣称蓝牙、内置麦克风或可用于电视 / 手机”不等于支持 Mac；录音笔也不能默认视为标准蓝牙麦克风，它可能只提供手机 App、文件传输、USB 音频或厂商私有链路。现阶段只记录连接失败结果，不在缺少日志和官方资料时推测根因。
  - 后续所有候选硬件先通过 Gate 0：在目标 macOS 版本上完成首次配对、稳定连接、断开和重连；只有 Gate 0 通过后，才继续验证 CoreBluetooth 服务、系统 HID 共存、ATVV 版本、codec、按键、音质和断连恢复。
  - G20BTS Plus / G20S Pro 系列已有 ATVV 实测，但已公开样机主要是 ATVV v0.4、8kHz；当前 App 只接受 16kHz，因此需要补充 8kHz 解码与输出、旧版帧格式和按键会话逻辑，适配成本高于 UR02。同名商品同时存在蓝牙、2.4GHz USB 和不同固件，不能仅凭商品标题承诺兼容。
  - Google Chromecast with Google TV Voice Remote 已有 ATVV v1.0 / 16kHz 实测，理论适配成本也较低，但供货状态不稳定，因此只作为备选兼容设备。
  - 本次测试的“小米蓝牙语音遥控器第三方兼容版”不能与另一个已单独验证可连接 Mac 的 RC001-MS 混为同一型号；完整证据、候选状态和真机验收清单保存在独立的产品资料工作区。
- [ ] 同一个遥控器在多台已配对 Mac 之间切换蓝牙连接（不使用网络转发） <!-- workshop:status=阻塞;priority=P2 -->
  - 当前结论：小米产品页与 Bluetooth SIG 官方产品记录均未声明 RC003 支持多个配对主机或主机切换；Apple 也明确说明 App 取消本地 CoreBluetooth 连接不保证底层物理链路断开。因此“在任意一台 Mac 的 App 中单击后直接抢占遥控器”目前不能判定为可实现。
  - 必须先用同一只 RC003 和两台 Mac 验证：分别配对后无需重新配对即可来回重连、当前 Mac 释放后遥控器会重新广播、macOS HID 不会立即抢回连接。通过后可实现“当前 Mac 释放 → 目标 Mac 连接”；任一关键条件失败则纯 App 方案不支持，并在此条目标明硬件限制。
  - 研究结论和真机验收步骤保存在独立的产品资料工作区。
